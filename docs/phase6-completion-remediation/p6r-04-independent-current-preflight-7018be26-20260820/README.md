# P6R-04 independent current Build/Export verification

Candidate: `7018be26acf2c1ebbeeffc86e6b4e2690352aef2`.

## Result

After one candidate build, fresh Studio was launched from this checkout exactly
as `node ./dist/cli/pokie.js --no-open`. The public rendered Design Game flow
created the deterministic Random project `P6R04 Fast Preflight` (seed `23`).
Its visible **Build/Export** Outcome library (republish) card completed one
artifact build in about 18 seconds.

The completed card retained the server preflight:

```text
Preflight: 21952 estimated item(s), 22478848 estimated bytes.
Warning: Exact generation will enumerate 21952 reel-stop combinations.
```

It also rendered `Built to …` and kept **Open as Project** and **Add to
Projects** enabled. The actions were inspected without opening or registering
the temporary output, avoiding an unnecessary persistent host-side artifact.

The first rendered Create Project click was visibly unaccepted (the enabled
control remained and no workspace appeared), so one safe retry was made. A
separate early driver selection completed the TypeScript sibling card; the
retained screenshot and transcript are solely the subsequent, exact Outcome
library card proof above.

## Retained evidence

| Surface | File | SHA-256 |
| --- | --- | --- |
| Completed Build/Export card | `build-export-complete-preflight.png` | `b5603e1c2693cd787a58f35890fd1d9620da19423672f172f4fb2f0fe73a7776` |

Only this README, a concise rendered-control transcript, and one screenshot
are retained. Generated project/output trees, browser profiles, automation,
and raw logs are excluded.
