# P6-20 independent host verification — passed

POKIE candidate `8db4a363e9dbd404bd1084f336c5745fcf2a8b77` completed its
normal `npm run build` on Node `v24.18.0`. Its `npm pack --ignore-scripts`
tarball was SHA-256
`19242014e5634120d7125653c2828a8f155205d4caabc8bb186dcd6d0896b624`.

The exact clean companion checkout
`/home/stager/Work/sta-ger/pokie-examples` was confirmed at
`09a0889b8d335eeacbdb277c37376d97de96c268`. An archive of that exact commit
installed the packed candidate and passed its production `npm run build`, then
ran its real public `npm start` Vite workflow. Its visible Fixture Slot Play control rendered `A/C/A | A/A/C |
A/A/A`, credits `1004`, win `5`, and `5x`.

The exact candidate's generated Fixture Slot package, started with its real
`npm start` / `pokie dev` client workflow, rendered the same round. A fresh
headed Studio then used visible Projects controls to Detect, Register, and Open
that package; its visible Play and Replay flows both rendered the same
`fixture-round`, round 1 result. The two representative Studio captures are
checksummed below; all temporary packages, profiles, servers, and full logs
were removed after verification.
