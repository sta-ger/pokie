# P6V-03 final focused rendered-harness boundary

Candidate `b59ee5a4aea0f271fff7c14c23f292f52fce160e` remains an
ancestor of this evidence-only checkout. The retained controller proof was
rechecked before this run; `create-project-readiness.png` still has SHA-256
`a48e0c09e908413e35d2e5194b77c6aeba5c164f26f2b794b7be6e71466e6638`.

This invocation repaired the retained selector history in the one persistent
harness: label-associated `Blueprint JSON` lookup, successful-Apply remount,
and the rendered `Game basics` form-tab prefix. It made four isolated
HOME/XDG registry and visible Chrome-profile launches, each from this checkout
only as `node ./dist/cli/pokie.js --no-open`.

The first two runs reached JSON input and Apply but stopped in harness
postconditions for the off-viewport Form toggle; the third reached the same
point after a corrected Form-tab prefix. The final run reached the locally
rendered JSON editor after its one safe active-window retry, then did not
confirm focus for the off-viewport `Blueprint JSON` textarea. Therefore it
did not type a new model or emit Create Project, Save, Close, or Open. No
rendered Studio/product error appeared. This is a driver-inconclusive boundary,
not a product finding; no screenshot, project/output tree, profile, registry,
raw log, or harness source is retained as evidence.
