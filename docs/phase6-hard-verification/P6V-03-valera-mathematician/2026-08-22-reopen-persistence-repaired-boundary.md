# P6V-03 reopen persistence — repaired rendered-control boundary

Candidate `b59ee5a4aea0f271fff7c14c23f292f52fce160e` remains the
provenance-bound ancestor of this evidence-only checkout. The three controller
retained proof files remain present and checksum-valid:

| File | SHA-256 |
| --- | --- |
| `2026-08-22-exact-candidate-driver-boundary.md` | `f7c6fbcce451431ee291b493499e2254638810ca257faff56ab449ea4d73f9ca` |
| `2026-08-22-harness-recovery-closeout.md` | `a9651424c393d44413e723567a5ed9af82ac4e6f20591537df54a034e92abef2` |
| `create-project-readiness.png` | `a48e0c09e908413e35d2e5194b77c6aeba5c164f26f2b794b7be6e71466e6638` |

After rebuilding this source checkout, two fresh HOME/XDG registries and two
fresh visible Chrome profiles launched it only with `node ./dist/cli/pokie.js
--no-open`. The persisted harness was repaired in place before the runs: it
kept the semantic-prefix advanced-options control, required the visible,
hit-tested Form/JSON label, and made its one idempotent retry as an
active-visible-Chrome physical pointer click at that same rendered control.

In each run `New Blueprint` → `Recommended` → `Show advanced options` reached
the visible JSON control. The native JSON radio became selected, but neither
the original click nor the one safe physical retry rendered its local
`Blueprint JSON` editor during the bounded semantic wait. No Studio/product
error appeared. Therefore no edit, Create Project, Close, Open, or persistence
request was emitted.

The temporary registry, profile, Studio process, Chrome process, and generated
runtime directory were removed after each run. This remains a readiness/
driver-inconclusive rendered-workflow boundary, rather than a product finding.
No screenshot, generated project/output tree, browser profile, raw log, or
automation source is retained as evidence.
