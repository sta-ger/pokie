# P6V-03 — exact-candidate browser recovery

Candidate source: `69d0c518a088a2d414400c30782417fa1c2e2a36`.

The persistent harness rebuilt a detached checkout at that exact SHA and used
four new Studio `HOME`/registry plus Chromium-profile pairs. Each launch used
`node ./dist/cli/pokie.js --no-open` from that source checkout. The retained
transcript proves rendered Recommended, Random, Blank, and final Recommended
creation; therefore the prior fresh-project creation P1 did not recur.

The final run also reached rendered Valera model sections, wild/scatter edits,
and the native artwork flow. After the focused picker returned, its Symbol 1
row visibly exposed `Change` and `Remove` with no rendered alert, proving the
artwork operation succeeded despite an earlier bounded wait. The remaining
journey was not reached because the reusable harness's next selector treated a
native radio as a button; no product error was rendered and the four-launch
limit prevented a further repair.

| File | Purpose | SHA-256 |
| --- | --- | --- |
| `transcript.txt` | Concise exact-SHA rendered provenance and recovery boundary | `ea91f563377b1273b045bdc72490293fecfd8dc767ae3548909a972b48c651c8` |
