# P6V-03 focused rendered recovery closeout

Candidate code: `b59ee5a4aea0f271fff7c14c23f292f52fce160e`. The current
checkout contains only evidence descendants of that candidate; the retained
candidate-bound proof remains present and checksum-valid.

Four fresh HOME/XDG registries and visible Chrome profiles used the exact source
launch `node ./dist/cli/pokie.js --no-open`. The final fresh session repaired
the earlier driver causes (real active-window keyboard retry, active native
picker, semantic workflow controls, and actual visible picker options).

The final rendered journey recorded `valera-mathematician` / `Valera
Mathematician`, a fourth payline, normal symbol `N` with selected PNG artwork,
`A` Wild, `K` Scatter, Per-reel Reel Strip Modeler, Literal Reel 1 Preview and
Apply, and a second available bet `2`. It also configured Reel 2 as Generated
with counts `A=2, K=1, Q=1, J=1`, locked `A` at position `0`, added the rendered
fixed-length-2 single-stack rule, received `Generated successfully`, and
applied that draft. The rendered `K x3 payout` was changed to `7`.

Create Project was then emitted once. Although the harness's expected tab-role
marker did not arrive, the final rendered observation is an actual Valera
Workspace (Overview, managed Blueprint, ID, and Close project); no product
error was rendered. The fourth public-workflow launch was exhausted before its
close-and-Projects-reopen portion, so that portion is not reached. This is a
driver/readiness limit, not a product finding.

| Retained proof | SHA-256 |
| --- | --- |
| `create-project-readiness.png` (rendered Valera Workspace) | `a48e0c09e908413e35d2e5194b77c6aeba5c164f26f2b794b7be6e71466e6638` |

No registry/profile, generated project or output tree, raw full log, PID file,
or automation source is retained.
