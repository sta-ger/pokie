# P6V-03 persistence continuation: Create readiness boundary

Candidate ancestry and the retained `create-project-readiness.png` checksum
were rechecked before this focused recovery. A fresh HOME/XDG Studio registry
and Chrome profile launched this source checkout only through
`node ./dist/cli/pokie.js --no-open`.

The repaired persistent harness activated the visible native PNG picker,
verified its active window, selected the absolute PNG path, and then observed
the fifth (`N`) symbol row transition from `Select PNG` to its rendered local
`Change` control. It issued the rendered `Create Project` action once.

For the following 90-second semantic wait, Studio rendered neither the
`Close project` Workspace control nor a local product error. No second Create,
Close, Projects, or Open action was sent. This is a workflow-readiness
boundary, not a rendered product finding; the required save/close/reopen
persistence observation remains unreached. No profile, registry, project tree,
raw log, or automation source is retained as evidence.
