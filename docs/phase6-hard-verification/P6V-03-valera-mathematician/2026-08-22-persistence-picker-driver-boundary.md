# P6V-03 persisted-workflow recovery: picker driver boundary

Candidate provenance was rechecked: this evidence checkout is a clean committed
descendant of `b59ee5a4aea0f271fff7c14c23f292f52fce160e`.  The retained
`create-project-readiness.png` remains present with SHA-256
`a48e0c09e908413e35d2e5194b77c6aeba5c164f26f2b794b7be6e71466e6638`.

Four isolated HOME/XDG registries and Chrome profiles launched this source
checkout only with `node ./dist/cli/pokie.js --no-open`.  The repaired stable
harness entered and applied the rendered Valera JSON blueprint.  It then used
the rendered `Symbol 5 id` row's `Select PNG` control; this row is the applied
blueprint's `N` symbol.  On the last permitted launch it located the visible
Zenity window, activated it, verified it was active, and entered the absolute
PNG path.

The harness's picker-completion predicate incorrectly let `xdotool search`
return its ordinary no-window exit as an exception.  It therefore timed out
without inspecting the next rendered artwork/result state.  No rendered Studio
error, Create/Save, Close, or Open request occurred in this recovery.  This is
a host-driver boundary, not a product finding; the persistence portion remains
unreached.  No generated profile, registry, project/output tree, raw log,
automation source, or screenshot is retained as evidence.
