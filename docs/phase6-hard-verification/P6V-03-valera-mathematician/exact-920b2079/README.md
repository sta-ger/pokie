# P6V-03 exact-SHA rendered recovery run — bounded evidence

Candidate source: `920b2079acde4a581bd9b520df38aa3268753205`.
Evidence commit parent: `8469e63bbacfe32d5294ca1837b675f63e005f56`.
Before each Studio launch the harness verified that this candidate was the merge
base and that the descendant changed only this evidence directory. One
`npm run build-cli` preceded both launches. Each launch used
`node ./dist/cli/pokie.js --no-open`, a new disposable Studio HOME/XDG registry,
and a new Chromium profile.

The retained first launch repaired the Literal restoration selector and reached
the editor's local Literal preview and Apply state. Its project-save readiness
wait matched descriptive body text rather than the actual navigation control.
The second, final permitted fresh launch repaired that readiness check and
visibly completed Recommended, Random, Blank, final Recommended; all modelling
sections; valid identifiers; A wild/K scatter; focused native-picker artwork;
literal A addition; Symbol weights A=2; Literal restoration; local `Literal
strip` preview; Apply; project creation; Play `Find any win`; 100-round
Simulation; Replay; exact Outcome Library generation; and Stake Engine export.
The transcript shows the rendered actions. The harness advanced past each
operation only after its specific local success/error state; no rendered product
error was observed.

After the successful export it selected `Close project`, then incorrectly tried
to locate Home's `Projects` navigation as an ARIA tab. That host selector
failure occurred after Studio had accepted Close, and the two-launch allowance
was exhausted. Therefore save/reopen, artwork persistence after reopening, and
the final reopened usability check remain unverified. This is driver-inconclusive
evidence, not a product finding.

| File | Rendered proof | SHA-256 |
| --- | --- | --- |
| `01-literal-reels.png` | Literal reel editor after adding visible `A` | `4b3ba471b69ebd543f38baa98f0a9ecfea52c31a90845830cc3a9bc0275ec3e8` |
| `02-symbol-weights.png` | Generated reel Symbol weights after adding `A=2` | `5e763ab24293dae86974e12ad7d65a5c5f09c0689d4aedb4e0f0a7e808a07bf7` |
| `03-literal-round-trip-applied.png` | Restored literal draft at Done after local preview and Apply | `c8fd239188af68d1804d817a6ac3d95efd5b2809bf86dfa663a441894782e709` |
| `04-workspace-success.png` | Exact outcome library plus Stake Engine export success | `82db1700e19ab896651bcbd50d81cb8a20afe4bca41784333c6494a43c3fbdb3` |
| `transcript.txt` | Bounded timestamped control and local-state record | — |
