# P6V-03 independent browser rerun — driver inconclusive

Candidate product: `a407378ba51e91dcbb5f6abddc7a07e4ed140535`. The checked-out descendant contains evidence changes only; its source tree matches the candidate. The retained exact-candidate/fresh-registry criterion was rechecked before this focused recovery.

Two additional fresh visible Studio launches used exactly `node ./dist/cli/pokie.js --no-open`, each with a separate HOME/XDG registry, Documents directory, and Chromium profile. Both successfully used rendered tabs and controls for the Valera metadata, fourth payline, A Wild, K Scatter, and `Select PNG`. The second additionally confirmed that the native picker process was alive before visible focus, path entry, and confirmation.

Neither native-picker interaction returned the rendered artwork `Change` state or a rendered Studio error in its bounded observation. Thus no preview, save, Workspace, close/reopen, Play, Simulation, Replay, outcome-library, or Stake Engine export request could be reached; the two-launch allowance is exhausted. This is driver-inconclusive, not a product finding. `ACTION-TRANSCRIPT.txt` is the bounded record; temporary profiles, registries, native-picker processes, automation, generated trees, and full logs were removed.
