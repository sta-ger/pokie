# P6V-03 independent browser rerun — inconclusive

Candidate product: `ca14ba14b334fd2a041fe09c3f16024fe8d04fe5`. The candidate was rebuilt once. The retained fresh-registry proof remains truthful. This focused recovery made two further fresh visible launches, each with its own Studio HOME/XDG registry and Chrome profile, using exactly `node ./dist/cli/pokie.js --no-open`.

The retained fresh-registry proof remains truthful. In this focused recovery, two further fresh visible candidate launches used coordinate-level interaction with rendered accessibility targets rather than the prior brittle tab text selector. The first passed the formerly blocked **Layout** transition, added the fourth payline, and reached the Symbols controls; its proof helper incorrectly expected an ARIA-only payline label as visible text, so that first run made no product assessment past that state. The second independently repeated the Layout/Paylines, Wild, and Scatter edits, opened the real visible native PNG picker, and visibly supplied a PNG path. Studio then rendered neither the expected artwork **Change** state nor a product error during the bounded semantic wait. No save, Workspace, close/reopen, Play, Simulation, Replay, outcome-library, or Stake-export request was emitted.

This is a driver/UI-bridge inconclusive boundary, not a product defect. The two-launch allowance is exhausted. `ACTION-TRANSCRIPT.txt` is the bounded proof; temporary profiles, registries, generated project/output trees, browser data, full logs, screenshots, and the temporary driver source were removed.
