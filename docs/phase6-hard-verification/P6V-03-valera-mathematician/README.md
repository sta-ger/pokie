# P6V-03 independent browser rerun — driver inconclusive

Candidate product: `ca14ba14b334fd2a041fe09c3f16024fe8d04fe5`; this evidence-only descendant contains no product-source change. The retained candidate-bound README and transcript remain present and truthful: their isolated registries and browser profiles launched this checkout with exactly `node ./dist/cli/pokie.js --no-open`.

This focused recovery used the two permitted fresh visible Studio launches, each with a separate HOME/XDG registry and Chrome profile. The first reached Recommended, Valera metadata, and the rendered fourth-payline action, then its driver rejected the valid terminal status `Valid, with warnings` because it recognized only `Valid —`. The second repaired that parser and reached rendered Recommended, Valera metadata, fourth payline, symbols, reel-strip, changed paytable payout, available-bet addition, and one `Create Project` action. Each local edit visibly reached a terminal valid state (some with the expected payline warnings).

After `Create Project`, the second run did not render the required Workspace signature before its bounded semantic wait expired; the harness recorded neither a Workspace nor a rendered product-error state. It did not issue a duplicate save. The two-launch allowance is exhausted, so close/reopen, Play, Simulation, Replay, outcome-library, and Stake-export remain unverified. This is driver-inconclusive—not a product finding. `ACTION-TRANSCRIPT.txt` contains the bounded rendered-control record; temporary profiles, registries, generated project/output trees, browser data, screenshots, raw logs, and driver source are removed.
