# P6V-03 independent browser rerun — finding

Product candidate: `384750860547c312d67d10b913fa642470cbaad1`.

This independent rerun used one fresh `HOME`/XDG Studio registry and one fresh
visible Chrome profile. Studio was launched once from this checkout with exactly
`node ./dist/cli/pokie.js --no-open`; no installed self-dependency, private
Studio action, DOM/state injection, generated project/output tree, profile, raw
log, PID, or automation source is retained.

The rendered transcript records every requested literal-model control outcome:
layout omission/add-payline, normal symbol and PNG picker, literal reel preview,
generated-draft constraint and stack controls (then discard), bet, mode, and
free-games mechanic. The recommended Valera project then saved and opened
Workspace.

Finding: Design Game accepts a duplicate available bet, reports it only as a
warning, and permits project creation. Opening the rendered Play surface then
crashes the Studio route with Mantine's `Duplicate options are not supported`
error for value `"2"`. Play win/feature, Simulation, Replay, Outcome Library,
and Stake Engine could not be reached after that rendered application error.

| Proof | SHA-256 |
| --- | --- |
| `play-duplicate-bet-error.png` | `c2e9cfe81b3ac00062bb00889a9683b27c2a95b8b3a3da2a80759ee481398bf9` |

`ACTION-TRANSCRIPT.txt` is the concise rendered-control record. The retained
PNG is 110,339 bytes; the evidence delta contains three files and no generated
project or runtime artifact.
