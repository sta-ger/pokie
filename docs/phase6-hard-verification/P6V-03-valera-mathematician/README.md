# P6V-03 independent browser rerun — driver inconclusive

Candidate product: `a407378ba51e91dcbb5f6abddc7a07e4ed140535`. This evidence-only descendant has no product-source difference from the candidate. The retained fresh-registry/candidate-command evidence remains present and was rechecked before the focused recovery.

The candidate was built once. Two fresh visible Studio launches then used exactly `node ./dist/cli/pokie.js --no-open`, each with separate HOME/XDG registry, Documents directory, and Chromium profile. In the first, rendered controls set the Valera metadata, fourth payline, A Wild, K Scatter, and opened `Select PNG`. Zenity was visibly present; direct keyboard input to that actual chooser window closed it, but no rendered artwork `Change` state or Studio error was subsequently confirmed. The second reached the same rendered `Select PNG`; its chooser process existed but had not yet mapped a visible window when the driver attempted to bind to it.

No preview, save, Workspace, close/reopen, Play, Simulation, Replay, outcome-library, or Stake Engine export action was emitted after the unresolved picker boundary. The two-launch allowance is exhausted. This is driver-inconclusive rather than a product finding. `ACTION-TRANSCRIPT.txt` is the bounded record; temporary profiles, registries, native-picker processes, automation, generated trees, and full logs were removed.
