# P6V-03 independent browser rerun — driver inconclusive

Candidate product: `ca14ba14b334fd2a041fe09c3f16024fe8d04fe5`; this evidence-only descendant contains no product-source change. Retained candidate-bound proof remains present and truthful: its prior fresh Studio registries and Chrome profiles used exactly `node ./dist/cli/pokie.js --no-open` from this checkout.

For this recovery, `npm run build` completed once, then two additional fresh visible Studio launches each used a separate HOME/XDG registry and Chrome profile with that same exact candidate command. The second rendered run confirmed Valera metadata, the fourth payline (`Payline 4 reel 1 row`), Symbol 1 Wild, Symbol 2 Scatter, and opening the real Zenity PNG picker. The host window manager rejected native-window activation before physical location entry; consequently Studio displayed neither artwork **Change** nor a product error and no save or downstream action was emitted. The first launch's wait used non-rendered payline text, so it is a harness observation error rather than a product result.

No rendered product defect was observed. This invocation's two-launch allowance is exhausted at a host-driver boundary; save/Workspace/reopen/Play/Simulation/Replay/outcome-library/Stake-export remain unverified. `ACTION-TRANSCRIPT.txt` is the concise proof. Temporary profiles, registries, generated project/output trees, browser data, screenshots, raw logs, and driver source are removed after this record.
