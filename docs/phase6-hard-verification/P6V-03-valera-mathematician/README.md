# P6V-03 independent browser rerun — inconclusive

Candidate product: `ca14ba14b334fd2a041fe09c3f16024fe8d04fe5`. The candidate was rebuilt once. The retained fresh-registry proof remains truthful. This focused recovery made two further fresh visible launches, each with its own Studio HOME/XDG registry and Chrome profile, using exactly `node ./dist/cli/pokie.js --no-open`.

Both recovery runs reached **Recommended** and accepted the visible metadata edits. The first rendered **Layout** action again did not render Layout/Paylines or a product error. On the one permitted rendered retry in the second run, the still-visible tab had changed from `Layout` to the status-badged rendered text `Layoutvalid` while `aria-selected` remained `false`; the bounded retry driver could not then resolve that control. No project save or downstream request was emitted. This is selector/driver inconclusive, not a product defect; the two-launch allowance is exhausted.

`ACTION-TRANSCRIPT.txt` is the bounded proof. Temporary profiles, registries, generated project/output trees, browser data, logs, screenshots, and the temporary driver source were removed.
