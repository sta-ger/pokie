# P6V-03 independent browser rerun — driver inconclusive

Candidate product: `ca14ba14b334fd2a041fe09c3f16024fe8d04fe5`; this evidence-only descendant contains no product-source change. Retained candidate-bound proof remains present and truthful: its prior fresh Studio registries and Chrome profiles used exactly `node ./dist/cli/pokie.js --no-open` from this checkout.

In this focused recovery, two additional fresh visible Studio launches each used a separate HOME/XDG registry and Chrome profile with that same exact candidate command. Both rendered Valera metadata, the fourth payline (`Payline 4 reel 1 row`), Symbol 1 Wild, Symbol 2 Scatter, and the real Zenity PNG picker. The first selected its PNG but had no semantic artwork state in its bounded inspection. The second independently selected it and visibly rendered **Change** and **Remove** for Symbol 1; bounded browser diagnostics also recorded Studio's native browse, PNG-import, artwork-preview, and validation requests.

No rendered product defect was observed. The temporary proof probe nevertheless searched literal `\\b` text instead of a word boundary and terminated the second session after the successful artwork observation. No duplicate picker action was issued. The two-launch allowance was exhausted in that recovery, so save/Workspace/reopen/Play/Simulation/Replay/outcome-library/Stake-export remained unverified due to driver error, not a product failure.

This invocation rebuilt the exact candidate and used two further fresh visible Studio/registry/profile launches. Both again completed rendered metadata, fourth payline, Wild/Scatter, and native PNG selection; the second also reached generated-reel count entry. The first stopped because its locator looked for the modeler's visible label `Configure` instead of the actual rendered ARIA label `Edit or generate Literal or generated`; the corrected second run stopped at a generated-reel lock-symbol dropdown where the harness did not resolve the rendered option. Neither rendered a Studio error or emitted save/downstream actions. The launch limit now prevents a further recovery. `ACTION-TRANSCRIPT.txt` is the concise proof. Temporary profiles, registries, generated project/output trees, browser data, screenshots, raw logs, and driver source are removed after this record.
