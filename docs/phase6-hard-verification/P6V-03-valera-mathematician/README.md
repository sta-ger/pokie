# P6V-03 independent browser rerun — inconclusive

Product candidate: `1fd51406388799fdcf65873ed9a2674769859cbf`.

The candidate was rebuilt once. The retained, candidate-bound proof for a
fresh Studio registry and visible Chrome profile was verified before this
focused recovery rerun. Both permitted new public launches again used a fresh
registry/profile and exactly `node ./dist/cli/pokie.js --no-open` from this
source checkout.

The repaired harness addressed the recorded selector cause by activating the
rendered editor tabs by role/prefix and waiting for their local controls. It
reached `Design Game` → `New Blueprint` → `Recommended` and entered the
Valera metadata on each run. The first run's selected-tab state assertion was
incorrect for this Mantine build; the second used the corrected assertion but
the rendered Layout tab did not expose its local Paylines controls within the
bounded semantic wait. Neither run rendered a Studio/product error.

Because both allowed launches are now spent, the duplicate-bet/Play/feature,
Simulation, Replay, Outcome Library, and Stake Engine portions are not
reached. This is a driver-interaction inconclusive result, not a product
finding. See `ACTION-TRANSCRIPT.txt`; no generated project/output tree,
browser profile, raw log, automation script, or screenshots are retained.
