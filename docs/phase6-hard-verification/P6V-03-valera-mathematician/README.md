# P6V-03 independent browser rerun — finding

Candidate product: `a407378ba51e91dcbb5f6abddc7a07e4ed140535`. This evidence-only descendant has no product-source difference from the candidate. The retained candidate-command/fresh-registry proof was rechecked before this focused recovery.

Two fresh visible Studio launches used exactly `node ./dist/cli/pokie.js --no-open`, each with an isolated HOME/XDG registry, Documents directory, and Chromium profile. The first mapped the real Zenity picker, but the Xvfb window manager lacks EWMH activation; that was a driver limitation, not a product result. In the second, window-targeted ordinary keyboard input selected the PNG in the visible Zenity dialog and Studio rendered artwork `Change`.

Rendered controls then completed the Valera metadata, fourth payline, A Wild, K Scatter, generated Reel 1 configuration/preview/apply, A x3 payout, and an added bet. One rendered `Create Project` click reached the semantic Workspace signature (`Close project` and `Game Model`) and also rendered the generic error: `The project could not be completed. Try again. If it continues, choose the location again and retry.` The contradictory error after the Workspace-success signature is the observed P2 finding. The allowed launches are exhausted, so close/reopen and downstream surfaces were not reissued. `ACTION-TRANSCRIPT.txt` is the concise bounded record; temporary profiles, registries, generated trees, browser data, and raw logs were removed.
