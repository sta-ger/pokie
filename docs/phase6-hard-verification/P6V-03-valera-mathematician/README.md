# P6V-03 independent browser rerun — selector inconclusive

Product candidate: `1fd51406388799fdcf65873ed9a2674769859cbf`, an ancestor
of this evidence-only checkout. The retained fresh-registry/profile proof
remains truthful.

This recovery read the complete harness-failure history, built the candidate
once, and used fresh isolated HOME/XDG registries and visible Chrome profiles.
Every one of its four Studio launches used exactly
`node ./dist/cli/pokie.js --no-open` from this source checkout.

The final fresh run repaired the previous tab, native-radio and picker causes:
it sent the Valera metadata inputs, completed a fourth payline, a focused PNG
import, Wild/Scatter controls, literal Reel 1 add/Preview/Apply, a paytable
edit, and the rendered `Duplicate bet 1` action. The one visible `Create
Project` action then opened Workspace; one visible `Close project` action
returned to Home. The capture proves that Workspace transition, but it renders
the persisted identity as `Starter Slot`; it must not be read as proof that the
typed Valera metadata committed.

The harness next waited for the persisted project's `Open` action without
first selecting the visible Home Projects panel. No Studio error rendered; this
is a harness selector miss, not a product finding. Together with the
unconfirmed metadata commit, all four allowed launches are exhausted, so the
full Valera persistence journey, Play, feature behavior, Simulation, Replay,
Outcome Library, and Stake Engine remain unassessed. No generated
project/output tree, profile, raw log, PID file, or automation source is kept.

| Proof | SHA-256 |
| --- | --- |
| `workspace-and-persistence.png` (103,158 bytes; Workspace transition, `Starter Slot` identity) | `645918afbbc2240ac3b8b427736848ea5cc81246137e179861e05cacd71c9273` |
