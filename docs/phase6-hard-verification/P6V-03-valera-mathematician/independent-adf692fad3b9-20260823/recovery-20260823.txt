P6V-03 focused harness recovery — inconclusive
Candidate SHA: adf692fad3b98fe327f06f3c2de0101bbe334dd6

Fresh-start preconditions for every launch: candidate Studio was started from this
checkout with `node ./dist/cli/pokie.js --no-open`; each had a new Chromium
profile, HOME, XDG_CONFIG_HOME and Documents root.  Browser automation used only
rendered controls and local rendered transitions.

Observed in the final fresh journey:
- Recommended: changed id/name; automatic validation reported valid; Create
  Project reached its rendered Workspace; Close project returned to Home.
- Random: Generate, Use this blueprint, automatic validation and Create Project
  each reached their rendered success/workspace state; Close project returned Home.
- Blank: chose Blank and rendered edits to id/name, layout, three symbols,
  wild/scatter toggles, literal reels, paytable and bet.  Its rendered validator
  then correctly reported four empty literal reel strips.

The incomplete Blank state is harness-caused: its keyboard helper appended to
Mantine number/text controls instead of replacing their values, and its repeated
"Add symbol" lookup selected the first rendered reel action.  No rendered
product error was observed.  The public-launch allowance is exhausted, so this
does not prove creation of the Blank project, persistence, Play/Simulation/
Replay, Outcome Library, Stake Engine, artwork, generated reels, constraints,
or mechanics.
