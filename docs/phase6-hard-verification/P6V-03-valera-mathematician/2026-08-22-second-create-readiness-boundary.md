# P6V-03 persistence continuation: repeated Create readiness boundary

Candidate code `b59ee5a4aea0f271fff7c14c23f292f52fce160e` remains an
ancestor of this evidence-only checkout. Before the rerun, the candidate CLI
was rebuilt, the retained `create-project-readiness.png` was rechecked as
`a48e0c09e908413e35d2e5194b77c6aeba5c164f26f2b794b7be6e71466e6638`,
and the persistent harness was repaired to inspect the local Create Project
success/error/pending surface.

Two new, isolated HOME/XDG registries and Chrome profiles launched this source
checkout only as `node ./dist/cli/pokie.js --no-open`. Each stable run used the
rendered New Blueprint, Recommended, Form/JSON controls and the active,
visible native picker to apply the candidate-bound Valera model and select the
`N` artwork. Each rendered `Create Project` action was issued exactly once.

For each following 90-second semantic wait, Studio rendered neither the local
Workspace `Close project` control, a local `Saved to` result, nor a local
Create Project error alert. No duplicate Create, Close, Projects, or Open
action was sent. Persistence is consequently still not reachable; this is a
readiness-inconclusive boundary, not a rendered product finding. The temporary
registries, profiles, generated project trees, raw logs, and automation output
were removed or remain solely in the machine-owned harness workspace.
