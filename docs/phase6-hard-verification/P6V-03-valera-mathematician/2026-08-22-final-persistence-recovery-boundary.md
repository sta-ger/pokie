# P6V-03 final persistence recovery boundary

Candidate `b59ee5a4aea0f271fff7c14c23f292f52fce160e` remains an
ancestor of this evidence-only checkout. The retained rendered Workspace
proof, `create-project-readiness.png`, is still present and SHA-256-valid as
`a48e0c09e908413e35d2e5194b77c6aeba5c164f26f2b794b7be6e71466e6638`.

The candidate CLI was rebuilt once. A fresh HOME/XDG registry and fresh visible
Chrome profile then started this checkout only as `node ./dist/cli/pokie.js
--no-open`. The stable rendered-control harness repaired its CDP-port startup,
semantic advanced-options prefix, associated native JSON radio, active native
picker, and local Create-result inspection.

The first fresh run entered and applied the rendered Valera JSON, selected the
rendered `N` PNG through an activated, active-window-verified native picker,
and emitted `Create Project` exactly once. The local checking indication ended,
but neither `Close project`, a local success, nor a rendered product error
appeared during the bounded semantic wait. It did not send a duplicate Create,
Close, Projects, or Open action.

Two follow-up fresh profiles repaired the prior JSON mode transition with one
safe idempotent retry. In the last run the visible JSON radio still did not
render its local `Blueprint JSON` editor after that retry; no model edit or
persistence request was emitted. This is a driver/readiness-inconclusive
boundary, not an observed product failure. No profile, registry, project/output
tree, raw log, PID file, or automation source is retained as evidence.
