# P6V-03 bounded fresh-profile recovery — driver inconclusive

Candidate code checked: `a87e90d8ced5febdccd6979915c9b35a66795f7d`.
Evidence commit parent: `7fc18a576f9771f137d11f4e95b3a95c1cd65866`.

The retained screenshot files remained present and checksum-valid before this
recovery. `npm run build-cli` passed. A single stable rendered-control harness
then made four fresh HOME/XDG registry and headed-Chrome-profile launches of
exactly `node ./dist/cli/pokie.js --no-open` from this source checkout.

Each launch reached rendered `New Blueprint` then `Recommended`. The first
confirmed that CDP input did not change the rendered Game id. The next used the
history-required verified-active visible-Chrome keyboard retry; the third
attached to Chrome's visible initial page; the fourth scrolled the located
metadata control into view before the same one safe retry. In all four, the
Game id rendered value did not become `valera-mathematician`. No Create Project
request, later workflow operation, or rendered Studio/product error occurred.

The four-launch allowance is exhausted. This is a driver boundary, not a
product finding. Recommended/Random/Blank completion and persistence remain
unreached; no temporary registry/profile, generated project/output tree, raw
log, PID file, screenshot, or harness source was retained.
