# P6V-03 rendered persistence completion

Candidate code: `b59ee5a4aea0f271fff7c14c23f292f52fce160e`; this evidence-only
checkout is its descendant. After the retained fresh-profile checks were
verified, a new HOME/XDG Studio registry and Chrome profile launched only
`node ./dist/cli/pokie.js --no-open` from this checkout.

The visible Studio workflow created Valera Mathematician, then completed
`Close project` → `Projects` → `Open` → `Game Model`. In the reopened model,
the rendered controls separately confirmed:

- `valera-mathematician` / `Valera Mathematician`, 5 reels, 3 rows, and 4 paylines;
- the fourth payline values `0, 1, 2, 1, 0` in Layout edit mode;
- A as Wild, K as Scatter, and the selected A PNG artwork in Symbols edit mode;
- Per-reel literal Reel 1's first symbol `A`; and
- `K x3 payout` = `7` in Paytable edit mode.

The representative screenshot is a rendered, reopened Game Model state
(including its `Close project` control, per-reel projection, rendered A
artwork, and K × 3 input value 7). SHA-256:
`f7295f84d2a3e6dc2f4c1a2c69d3dd0df346308befebb267d77767df6c2242ae`.

No project tree, registry, browser profile, raw log, PID file, or harness is
included in the evidence.
