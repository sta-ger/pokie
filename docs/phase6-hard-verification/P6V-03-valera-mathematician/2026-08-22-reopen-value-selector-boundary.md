# P6V-03 reopened-value selector boundary

Candidate code: `b59ee5a4aea0f271fff7c14c23f292f52fce160e`, an ancestor of
this evidence-only checkout.  The controller-retained proof files remain
present and checksum-valid.

The final fresh, source-launched Studio session used a new HOME/XDG registry
and Chrome profile, launching only `node ./dist/cli/pokie.js --no-open`.  Its
rendered controls set `valera-mathematician` / `Valera Mathematician`, added
the fourth payline (`0, 1, 2, 1, 0`), set A Wild and K Scatter, selected A PNG
artwork through an active, verified native picker, chose the Per-reel Reel
Strip Modeler, entered and previewed a literal A, and set K x3 payout to 7.

`Create Project` rendered a Valera Workspace.  The visible flow then completed
`Close project` → `Projects` → `Open` → `Game Model`.  No Studio error was
rendered.  The harness's final exact body-text predicate for the reopened
model did not arrive within its bounded semantic wait, so this run does not
claim that all edited values were visibly reverified.  This is a selector
inconclusive boundary, not a product finding.  No profile, registry, project
tree, raw log, or automation source is retained as evidence.
