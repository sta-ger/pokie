# P6V-03 final focused recovery — driver inconclusive

Candidate checked: `a87e90d8ced5febdccd6979915c9b35a66795f7d` (an ancestor of this
evidence commit). `npm run build-cli` completed before the browser work. The
retained proof files remain present, with SHA-256 values
`645918afbbc2240ac3b8b427736848ea5cc81246137e179861e05cacd71c9273`
(`workspace-and-persistence.png`) and
`b80794689b1bf62d3fd5e5fcdf72b6ce32587c6d659d80bf1815c2fa88847150`
(`final-play-feature.png`).

The complete retained driver/readiness history was read before four fresh,
headed HOME/XDG registries and Chrome profiles. Each source-checkout launch
was exactly `node ./dist/cli/pokie.js --no-open`. A single stable rendered
control harness used browser pointer/keyboard events and, after a visibly
unaccepted idempotent choice only, one verified-active Chrome physical retry.

Launches reached the rendered Design Game and New Blueprint surfaces. Two
rendered `Recommended` actions removed their local choice control but never
rendered the required Game id input; the last fresh launch's rendered New
Blueprint action did not produce its dialog in the bounded semantic wait. No
Studio/product error, Create Project request, or downstream operation was
rendered. This is a driver/readiness boundary, not a product finding. The
four-launch allowance is exhausted, so Recommended/Random/Blank completion,
the omitted literal modelling path, and persistence are not reached.

No screenshot, project/output tree, browser profile, registry, raw log, PID
file, or automation source is retained.
