POKIE Studio
Design Game
Projects
Design Your Game

Start with the recommended playable model, then make it your own. Edit its layout, symbols, reels, paytable, and bets; select PNG artwork for a symbol in the Symbols section. Studio checks the model automatically; Create Project saves it and opens the Workspace for Game Model, Play, Simulation, and Replay.

Create Project

Show advanced options (JSON mode, load/save by path)

New Blueprint
Game basics
valid
Layout
valid
Symbols
valid
Reels
valid
Paytable
valid
Bets
valid
Reel generation

Optional — omit all three to use the engine's default weighted reel generator.

Default
Reel strips
Per-reel (Reel Strip Modeler)
Symbol weights

Each reel is independently a literal strip or a generated one (own length, seed, counts/weights, locked positions, constraints). Editing a reel here never touches the blueprint until you explicitly Apply it — Check & Preview always runs the same core generation/analysis API "pokie build" itself uses, purely in memory.

Select reel
Configure
Preview
Done

Reel 2

Copy
Literal
Generated
Add symbol
Preview
Validation

Checked automatically when the game model changes.

Valid — no issues found.

Game Model preview
Preview Game Model
Documentation
Docs index
Getting started
CLI reference
