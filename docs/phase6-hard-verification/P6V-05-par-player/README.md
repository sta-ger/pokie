# P6V-05 host verification closeout — inconclusive

Product content is bound to candidate `49d5fccc517f5a7f964ecc7fa32148edeb18d588`.
The required read-only `pokie-examples` checkout was independently verified clean at
`b7b043e0e722da917f1b60c4f107c8cc35fdd725`. This evidence-only descendant changes
only this README.

The retained physical fixture is present and unchanged:

```text
examples/parsheets/starter.par.xlsx
sha256 a2e88ad5962551e8be9b2710b141965cfbae354ca8e9f254b6e9f53a9f9b4924
```

In the retained fresh-profile candidate-build Studio journey, launched only as
`node ./dist/cli/pokie.js --no-open`, the real activated native picker selected that
workbook. Studio then rendered `Imported with warnings`, PAR provenance, and canonical
preview SHA-256 `ed4953d3c1e8bc2c8eaa6670bdbe3aee564a65c8245f2b52c7449e7e4e14f4cc`.
The visible Apply action succeeded, the managed Blueprint name visibly changed to
`PAR Sheet Starter round-trip`, and a real native Save picker produced the isolated
saved expected Blueprint with SHA-256
`43a1242ed00164a3699d081f1780f81a52db94fc78ebceae3c250eb2a2b1d00b`.

The repaired persistent harness could not resolve the remounted rendered `Apply / Export`
Stepper control as interactive. No rendered product error, failed export, or canonical
mismatch was observed. Therefore physical XLSX export, re-import, semantic comparison,
and the dependent exact-companion package/npm-start/Studio-Play/public-client-dev/Replay
parity matrix were not reached. This is selector-inconclusive, not a product finding.

In the focused recovery rerun, the Stepper binding was repaired to use the rendered
ordinal/label/description control text. A fresh isolated Studio journey again accepted
the exact visible `Browse…` action: its local availability request and native-browse
request were issued, after which Studio visibly rendered its `Server filesystem browser`
fallback. No native dialog or product error was rendered. Zenity itself is available on
the controller display, so this is a host/browser-driver inconclusive transition rather
than evidence of a rendered Studio defect. The required physical native-picker workflow
could not reach import in that fresh journey; the retained successful pre-Stepper proof
above remains the newest candidate-bound proof for import, diagnostic, preview, Apply,
edit, and managed save.

No runtime trees, browser profiles, generated workbooks, screenshots, raw logs, harness
source, symlinks, or generated output are retained. This README is the sole evidence
artifact.
