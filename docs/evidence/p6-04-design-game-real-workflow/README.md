# P6-04 Design Game Studio host verification

Candidate `8c888f6147c70b7d5c195266ee45c2c1201f72fd` was built and exercised
through a fresh local Studio server and a fresh Chrome profile using visible
rendered controls only.

The successful `workflow-browser-transcript.txt` and matching screenshots show:

- a Recommended project whose **Game name** was edited to `P6 Recommended
  Edited`, then created, registered, opened in Workspace, spun in **Play**, and
  completed a 25-round **Simulation**;
- the visible default deterministic Random seed `20260815` generated `Royal
  Grand Stampede`, which was then created, opened, spun, and simulated; and
- `restart-browser-transcript.txt` / `05-after-restart-projects.*` show both
  projects still registered with rendered **Open** actions after a clean Studio
  and Chrome restart.

`random-name-crash.*` captures a separate UI finding: typing into Random's
optional **Name (optional)** field causes the rendered client to fail with
`Cannot read properties of null (reading 'value')`. The source handler retains
the React event and reads `event.currentTarget.value` inside a deferred
functional state updater, after React has cleared `currentTarget`.

`managed-projects/` contains the actual Blueprint files generated by the UI;
`studio-config/pokie/projects.json` is the isolated persisted registry used for
the restart run. Terminal logs retain the build, Studio server, Chrome, and
browser-run output. The Chrome profile directories are retained in full so all
untracked evidence created beneath this requested evidence location is
preserved.
