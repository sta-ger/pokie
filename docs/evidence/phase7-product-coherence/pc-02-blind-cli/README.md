# PC-02 — fresh installed blind CLI pass

## Provenance and boundary

| Field | Value |
| --- | --- |
| Candidate checked before packaging | `250a1ab5693d1b7404d5911b297bdac204fbdb49` (clean worktree) |
| Pack/install provenance | `npm pack --pack-destination /tmp/pokie-pc02-jClS7o --json`; `pokie-1.3.0.tgz`, SHA-256 `1aca26eb6c1aa3f8480be92e48b3440c57ed07e2e9d3333ded5862958250d5bc` |
| Fresh consumer | `/tmp/pokie-pc02-install-hWzBzT`, created by `npm init -y` then `npm install --ignore-scripts /tmp/pokie-pc02-jClS7o/pokie-1.3.0.tgz` (exit 0; 99 packages) |
| Public entry used | README’s `npx pokie` route, then only the installed executable through `npx --no-install pokie`; `--version` → `1.3.0`, `--help` → exit 0 |
| Explorer boundary | Before recording findings, this explorer read only the public README installation/CLI guidance and rendered installed-CLI help. It did not read source, prior PC-02 evidence, audit scripts, implementation/fix reports, or known findings. |
| Retention | The consumer, packed tarball, temporary secret, servers, logs, browser-free outputs, and generated artifacts remain disposable `/tmp` material. This one README is the complete retained payload. |

`pokie --help` plus `--help` for `create`, `init`, `build`, `validate`, `inspect`, `sim`, `report`, `replay`, `serve`, `client`, `dev`, `edit`, `export`, `import`, `par`, `diff`, `generate`, `sample`, `fairness`, `certification`, and `reel` all exited 0. The commands below were selected as natural follow-ons from that rendered guidance, rather than from a prepared happy-path script.

## Action ledger

| Natural intent / exact reproducer | Observation and actual outcome | Exit / recovery | Severity |
| --- | --- | --- | --- |
| Create in a **relative path with spaces**: `npx --no-install pokie create 'Spaced Random' --random --seed 17 --out 'projects with spaces/random blueprint.json'` | Created the Blueprint; generated id remained `Spaced Random`, provenance `generator 1.1.0`, `default-line-pay`. `validate 'projects with spaces/random blueprint.json' --format json` was valid; `inspect` recognized the Blueprint. | 0 / 0 / 0 | — |
| Follow the create output’s suggested Build command for that spaced path. | The rendered instruction was `pokie build projects with spaces/random blueprint.json --target tsPackage --out <dir> --dry-run` (unquoted), which shell-splits the provided path. Copy/paste cannot work; user must discover quoting independently. | See **PC-05-CLI-03**. | P2 |
| Retry same create destination (stale file): `npx --no-install pokie create 'Spaced Random' --random --seed 17 --out 'projects with spaces/random blueprint.json'`. | Stated that the file exists and to choose another path/remove it. | 1; clear recovery. | — |
| Test build without writes: `npx --no-install pokie build 'projects with spaces/random blueprint.json' --target tsPackage --out 'outputs/game package' --dry-run`. | Valid Blueprint; explicitly “no files written”; destination did not exist afterward. | 0 | — |
| Build the same relative source to a **spaced output**: `npx --no-install pokie build 'projects with spaces/random blueprint.json' --target tsPackage --out 'outputs/game package'`. | Real `tsPackage` published to absolute `/tmp/pokie-pc02-install-hWzBzT/outputs/game package`; package `validate` and `inspect` both passed. | 0 / 0 / 0 | — |
| Test an **absolute package path**: `validate '/tmp/pokie-pc02-install-hWzBzT/outputs/game package' --format json`. | Valid `Spaced Random` package. | 0 | — |
| Non-empty stale destination: create `outputs/occupied destination/pre-existing-user-file`, then `npx --no-install pokie build 'projects with spaces/random blueprint.json' --target tsPackage --out 'outputs/occupied destination'`. | Safely refused; says target must be missing or empty and the sentinel was retained. | 1; exact recovery is a new `--out` or removal. | — |
| Missing/wrong inputs: `npx --no-install pokie validate 'does not exist' --format json`; `npx --no-install pokie build 'projects with spaces/random blueprint.json' --target unsupported`. | Missing path emitted structured `pokie-package-load-failed`; invalid target listed the four supported targets. | 1 / 1; no crash. | — |
| Simulate and report: `npx --no-install pokie sim 'outputs/game package' --rounds 12 --seed pc02 --format json --out 'outputs/simulation report.json'`; `npx --no-install pokie report 'outputs/simulation report.json' --format markdown --out 'outputs/simulation report.md'`. | 12 rounds, reproducible seed, report written; Markdown rendered the same summary and warning. | 0 / 0 | — |
| Replay: `replay 'outputs/game package' --seed pc02 --round 1 --out 'outputs/replay.json'`. | Round descriptor written. | 0 | — |
| Direct Blueprint follow-on: `sim 'projects with spaces/random blueprint.json' --rounds 2 --seed pc02-blueprint`. | Simulated successfully even though `inspect` says one “must” build before simulate. | 0; see **PC-05-CLI-01**. | P2 |
| Library route: `npx --no-install pokie build 'projects with spaces/random blueprint.json' --target outcomeLibrary --sample 20 --seed pc02-library --out 'outputs/outcome library'`; then `validate 'outputs/outcome library' --deep --format json`, `sample 'outputs/outcome library' --mode base --seed pc02-sample`, `sim 'outputs/outcome library' --mode base --rounds 12 --seed pc02-outcome --format json --out 'outputs/library simulation.json'`, `report 'outputs/outcome library' --format json --out 'outputs/library report.json'`, and `replay 'outputs/outcome library' --mode base --seed pc02-replay --round 1 --out 'outputs/library replay.json'`. | Built bounded 20-draw library (full source estimate: 17,210,368 combinations); deep validation, sampling, simulation, report, and exact replay all passed. Mode was `base`; library SHA-256 `0d1b3ffa02c6591ee5bcf9c0b1f6045221f01bbb787deb8e86f28e7b4fdeeef3`. | 0 for every action. | — |
| Compare and generation follow-ons: `npx --no-install pokie diff 'outputs/simulation report.json' 'outputs/simulation report.json' --format json --out 'outputs/diff.json'`; `generate 'outputs/game package' --estimate --format json`; `reel generate 'projects with spaces/random blueprint.json' --seed 17 --format json`. | Identical diff (`changed: false`); generation estimated exact 17,210,368-space strategy; five generated reel previews succeeded without applying writes. | 0 / 0 / 0 | — |
| Export then import: `npx --no-install pokie export 'projects with spaces/random blueprint.json' --to outcomes --out 'outputs/export outcomes' --dry-run`; `export 'projects with spaces/random blueprint.json' --to adapter --out 'outputs/stake export'`; `import 'outputs/stake export' --out 'outputs/stake import'`. | Outcome dry run wrote nothing. Stake export and import succeeded; import explicitly warns its reconstructed library hash differs because round id, real breakdown, and POKIE version are unrecoverable. | 0 / 0 / 0 | — |
| Use advertised JSON format: `import 'outputs/stake export' --out 'outputs/stake import' --format json`. | Top-level `import --help` advertises `--format`; delegated public handler rejects it as unknown, and changes the usage to `pokie stakeengine import`. Retrying the immediately prior command without `--format` succeeds. | 1 then 0; see **PC-05-CLI-04**. | P2 |
| Try a plausible incompatible import: `import 'outputs/outcome library' --out 'outputs/incompatible outcome import'`. | Only says `stakeengine-import-index-missing` and mentions no artifact kind, supported input, or next step. | 1; see **PC-05-CLI-02**. | P2 |
| PAR round trip: `npx --no-install pokie par export 'projects with spaces/random blueprint.json' --out 'outputs/random.par.xlsx'`; `par import 'outputs/random.par.xlsx' --out 'outputs/from par.json' --format json`; `validate 'outputs/from par.json' --format json`. | XLSX created; import includes matching provenance and produces a valid Blueprint. | 0 / 0 / 0 | — |
| `init` path and restart: `init 'outputs/init with spaces' --no-install`; rerun it; then ordinary `init 'outputs/normal init'` and validate. | `--no-install` created the scaffold but predictably failed build (`tsc: not found`); rerun preserved files but repeated the same recoverable error. Ordinary init installed/prepared and validated a `normal-init` package. | 1 / 1 / 0 / 0 | — |
| Serve, cancellation, restart: `serve 'outputs/game package' --port 0`; after rendered `POKIE dev server listening on http://127.0.0.1:<port>`, interrupt; restart same command and await a second rendered listener. | First listener reached `44917`; after intentional SIGINT, restart reached `42205`. No rendered product error. | ready / cancelled / ready | — |
| Combined public client path: `./node_modules/.bin/pokie dev 'outputs/game package' --port 0 --client-port 0 --no-open`; cancel after both visible listeners. | API listener and browser-client listener rendered (`42367`, `38587`); direct SIGINT ended with exit 0. This covers `client` through the public `dev` workflow without launching a browser. | 0 | — |
| Provably Fair follow-on: `fairness seed-commit`, `commit --client-seed pc02-client --nonce 0 --source 'outputs/outcome library' --mode base`, `reveal`, then `verify`. | All four stages completed and verification said the proof was successful. The temporary secret/proof is not retained. | 0 / 0 / 0 / 0 | — |
| Interactive/edit, Studio open, and certification. | `edit --help` states it is interactive; the CLI has no `open` command (README’s `pokie .` is Studio, outside this CLI-only run). `certification build --help` requires a config schema supplied only by an external document. They were recorded as publicly described but not safely fabricable from installation guidance and CLI help alone; no source, doc, or scripted input was used to fake them. | Help 0; concrete blind-boundary unreachability. | — |

## PC-05 handoffs (no remediation)

| Handoff | Classification | User impact and exact reproducer | Severity |
| --- | --- | --- | --- |
| PC-05-CLI-01 | Misleading language / false internal prerequisite | `inspect 'projects with spaces/random blueprint.json'` says build is required before simulation, but `sim 'projects with spaces/random blueprint.json' --rounds 2 --seed pc02-blueprint` succeeds. It sends a new user on an unnecessary detour. | P2 |
| PC-05-CLI-02 | Incompatible artifact / recovery failure | `import 'outputs/outcome library' --out 'outputs/incompatible outcome import'` returns only a missing internal `index.json`; it neither identifies the provided Outcome Library nor says import accepts a Stake export/PAR workbook. | P2 |
| PC-05-CLI-03 | Spaced-path duplication / copy-paste failure | Successful `npx --no-install pokie create 'Spaced Random' --random --seed 17 --out 'projects with spaces/random blueprint.json'` prints `pokie build projects with spaces/random blueprint.json --target tsPackage --out <dir> --dry-run`. Directly copying it makes the shell split the project name. | P2 |
| PC-05-CLI-04 | Public grammar duplication / recovery failure | `import --help` advertises `--format`, while `import 'outputs/stake export' --out 'outputs/stake import' --format json` rejects it. Removing a documented option is the only successful recovery. | P2 |

No product code, tests, generated project trees, browser profiles, automation, raw logs, PID files, or artifacts were retained or changed. The concise package checksum above is the artifact provenance; this evidence delta is one text file well below both file and payload limits.
