# PC-05 capability matrix

This is the acceptance contract for product coherence, frozen after the PC-02,
PC-03, and PC-04 evidence.  The machine-readable artifact vocabulary is
[`artifact-registry.json`](artifact-registry.json).  A dash means the surface
does not expose the operation; it is not evidence that the operation works by
some other route.

| Domain operation / user goal | CLI | Studio | Public docs | Shared contract / defaults | Prerequisite and validation | Diagnostic semantics | Owner |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Create an editable game design | `create` (`--blank`, `--random`, wizard) | Home Design / create | `getting-started.md` | `GameBlueprint`; default output derives from id | valid blueprint required before dependent builds | file conflict: no overwrite; invalid fields are actionable | PC-09 docs/surface sweep |
| Scaffold a ready-to-run project | `init <directory>` | Design / Build project creation flow | `getting-started.md`, `game-packages.md` | starter Blueprint materialized as a local TypeScript package with the running POKIE version | chosen empty/safe directory; local dependency installation | unsafe/existing destination is refused before scaffold replacement; installed version is stated | PC-16 consolidation sweep |
| Edit a design | `edit` | Design editor | `getting-started.md` | canonical Blueprint JSON | load then validate before save/build | edited source invalidates results | PC-10 Studio recovery sweep |
| Recognize/open a project | `inspect`, path-taking commands | Projects / register/open | `docs/README.md`, `game-packages.md` | `ProjectTargetResolver`, `ProjectType` | resolver classifies Blueprint/package/library/Stake/PAR/WASM | name the recognized type and next action; do not leak parser internals | PC-06 diagnostic sweep |
| Exchange a PAR workbook | `par export`, `par import`; `export --to workbook` | PAR import/export panel | `stake-engine-import.md` is not authoritative; PAR docs/examples are | `ParSheetExporter`/`ParSheetImporter`; XLSX | Blueprint → workbook → Blueprint | workbook is exchange-only; say import-before-run | PC-11 PAR conversion sweep; PC-15 public CLI sweep; PC-16 Studio sweep |
| Build a runnable package | `build --target tsPackage` | Build/Export artifact target | `game-packages.md` | `ArtifactBuilderRegistry`, `BUILD_PRODUCT_MATRIX`; default sibling `tsPackage` | Blueprint; `GameBlueprintValidator`, materialization | unsupported source uses shared matrix diagnostic | PC-15 public CLI sweep; PC-16 Studio sweep |
| Generate raw weighted outcomes | `generate --out <file>`; legacy/internal `outcomelibrary generate --out <file>` | no raw-file-only Studio route: Studio Generate immediately materializes a bundle | `outcome-library-bundle.md` | `generateExactWeightedOutcomeLibrary` produces one `WeightedOutcomeLibrary` JSON value; `--resume` persists an `ExactEnumerationCheckpoint` only on cancellation | runnable package must support the selected exact/sampled enumeration; checkpoint resumes only the same compatible generation | raw JSON is an intermediate result, not a native bundle; cancellation says whether a checkpoint was written and how to resume | PC-09 Outcome Library sweep; PC-15 public CLI sweep |
| Materialize a canonical Outcome Library bundle | `build --target outcomeLibrary` from a recognized compatible project, `export <config.json> --to outcomes` from a descriptor; legacy/internal `outcomelibrary build` | Outcome Libraries / Generate (raw generation plus immediate bundle write) and Build-Export | `outcome-library-bundle.md` | bundle manifest/per-mode index/JSONL; the descriptor-based export route consumes named raw `WeightedOutcomeLibrary` JSON, while target build uses a recognized compatible project source | bundle writer/deep validation; raw JSON alone is not sample/serve/replay capable | destination and source/provenance failure identifies materialization; do not advertise `generate` as already having built a directory bundle | PC-09 Outcome Library sweep; PC-15 public CLI sweep; PC-16 Studio sweep |
| Inspect, sample or analyze precomputed outcomes | `inspect`, `sample`, `report --out`, `diff --out`; legacy/internal `outcomesource inspect/sample/diff --out`, `stakeengine analyze/diff --out` | Outcome source overview / Inspect, Sample, report comparison | `outcome-library-bundle.md`, `stake-engine-standalone.md` | `OUTCOME_SOURCE_*` capabilities and standalone Stake analyzer | native library can sample; native library and Stake export can analyze/diff | clearly distinguish read-only Stake source from drawable native library; persisted analyses/diffs are result records, never source projects | PC-06 diagnostic sweep |
| Export Stake files | `build --target stakeAdapter`, `export --to adapter`; legacy/internal `stakeengine export` | Build/Export and Stake Export | `stake-engine-export.md` | registry plus `StakeEngineExporter` | library, or Blueprint/package via managed compatible library | show prerequisite generation and output conflict, not a second incompatible workflow | PC-10 Stake export sweep; PC-15 public CLI sweep; PC-16 Studio sweep |
| Import Stake files | `import` (public); legacy/internal `stakeengine import` | Project registration/open only; no equivalent conversion action | `stake-engine-import.md` | `StakeEngineImporter` reconstructs an Outcome Library plus re-export-config and optional source-provenance companions | POKIE-produced export needs `pokie-manifest.json`; generic Stake is analysis-only | incompatible library input must name its type, supported imports and recovery | PC-06 diagnostic sweep; PC-16 consolidation sweep |
| Validate artifacts | `validate --out <file>` writes a `ValidateReport`; legacy/internal `outcomelibrary validate`; `certification verify` | Blueprint check, Outcome/Stake export validate, certification source validate | artifact-specific docs | resolver plus validators; `--deep` applies to libraries | target-specific validation only | state what was checked and what is not checked (notably WASM/runtime); a saved ValidateReport is not a project input and must be rerun after source changes | PC-11 Studio validation sweep; PC-06 CLI contract sweep |
| Run/play a game | `serve`, `client`, `dev` | Play | `game-packages.md`, `pregenerated-runtime.md` | `RUNTIME_EXECUTE_CAPABILITY` or native outcome-source sampling | runnable package or native Outcome Library, mode when required | package execution versus pre-generated draw must remain explicit | PC-08 Studio parity |
| Simulate | `sim --out`; `sim --mode all` produces a per-mode SimulationReportSet | Simulation | `getting-started.md`, `pregenerated-runtime.md` | package runtime or `OUTCOME_SOURCE_SAMPLE_CAPABILITY`; report JSON/report-set | package or native library; seeded native simulation is replayable | reject missing native-library seed/mode when exact replay is promised; never invent a blended multi-mode RTP | PC-06 contract sweep |
| Render simulation or outcome-source analysis | `report --out <file>` (`--format json` re-emits the parsed SimulationReport/SimulationReportSet; source analysis has its own result type) | Simulation results only | `getting-started.md`, `stake-engine-standalone.md` | simulation-report parser / outcome-source analyzer | simulation report/set or analyzable native/Stake source | a persisted rendering/analysis is presentation, never a project artifact; source-specific JSON is not interchangeable with a simulation report | PC-08 Studio parity |
| Compare reports or outcome sources | `diff --out <file>`; legacy/internal `outcomesource diff --out`, `stakeengine diff --out` | Simulation results only | `getting-started.md`, `stake-engine-standalone.md` | simulation differ/set differ, outcome-source differ and standalone Stake differ | like-with-like simulation reports/sets, or two analyzable sources of the same command contract | persisted diff is historical when either input changes; source diffs never create a drawable source and create-only branches refuse overwrite | PC-06 diagnostic sweep |
| Replay a result | `replay --out` | Replay (seed, artifact, session spin, simulation sources) | `pregenerated-runtime.md`, `round-artifacts.md` | exact native descriptor versus best-effort package replay | library id/hash/mode/seed/round must match for exact result | mismatch must identify stale/incompatible provenance; package replay says best effort | PC-06 provenance sweep |
| Certify an Outcome Library | `certification build`, `certification verify` | Certification build/inspect/export | `certification-evidence-bundle.md` | `CertificationEvidenceBundle*` | native library and config; verify also requires live source | build/source validation differs from independent post-build verification | PC-11 Studio validation/certification sweep |
| Prove fairness | `fairness seed-commit/commit/reveal/verify` | Provably Fair configure/generate/verify | `provably-fair.md` | commitment/proof validators and verifier | private seed file, native library, mode; matching commitment/source for verify | never disclose private seed; mismatch fails closed | PC-06 provenance sweep |
| Deploy an external format | no first-class public CLI command | Export/Deploy | `external-adapter-sdk.md` | `ExternalDeploymentService` + registered target | native library and target capability | stage-specific diagnostic; private RGS integration is not implied | PC-08 Studio/public parity decision |
| Generate/inspect reel strips | `reel generate` | Blueprint reel tools | `reel-strip-generation.md` | `resolveReelStripGeneration` | Blueprint constraints | generation failure is design validation, not runtime failure | PC-09 docs/surface sweep |
| Assess WASM compatibility | `inspect` resolves compatible sidecar; no `wasm` command | register/open/inspect metadata only | `wasm-compatibility-boundary.md` | `WasmProjectTargetAdapter`, manifest validator | compatible `.wasm` + sidecar manifest | inspection-only; explicitly reject build/run/simulate/validate-logic promises | PC-13 WASM boundary sweep |

## Public route inventory and aliases

The public CLI inventory is the command tree registered by `registerCliCommands()`.
The implementation-only names below are recorded only to make duplicate delegation
auditable: they are deliberately not supported public commands. The registered
`__studio` adapter is likewise internal; its concrete `studio` launcher is not a
public CLI alias. A row in the main matrix names the destination semantics for
every public route.

| Public CLI route | Alias / duplicate route treatment | Matrix operation |
| --- | --- | --- |
| `build` | target-specific product remediation belongs to PC-09 (Outcome Library), PC-10 (Stake) and PC-11 (PAR); public CLI aliases are PC-15 and Studio duplicates are PC-16 | Build a runnable package / Materialize a canonical Outcome Library bundle / Exchange a PAR workbook / Export Stake files |
| `certification` → `certification build`, `certification verify` | distinct build and independent verification routes | Certify an Outcome Library |
| `client`, `dev`, `serve` | `dev` composes the two local browser/runtime routes | Run/play a game |
| `create`, `edit`, `init` | design source, edit, and ready-to-run scaffold routes remain distinct goals | Create an editable game design / Edit a design / Scaffold a ready-to-run project |
| `diff`, `report` | `diff --out` persists one of the separately typed comparison results; `report --out` persists a simulation rendering or outcome-source analysis. Legacy `outcomesource diff` / `stakeengine analyze|diff` delegate to these analysis goals | Inspect, sample or analyze precomputed outcomes / Render simulation or outcome-source analysis / Compare reports or outcome sources |
| `export` | target-specific product remediation belongs to PC-09 (Outcome Library), PC-10 (Stake) and PC-11 (PAR); public CLI aliases are PC-15 | Materialize a canonical Outcome Library bundle / Export Stake files / Exchange a PAR workbook |
| `fairness` → `fairness seed-commit`, `fairness commit`, `fairness reveal`, `fairness verify` | commit/reveal lifecycle, not conversion aliases | Prove fairness |
| `generate` | canonical public raw `WeightedOutcomeLibrary` JSON generation. Its `--out` file is an input to a later descriptor-based `export --to outcomes` bundle materialization; it does not create a bundle. Legacy `outcomelibrary generate|build` is not public | Generate raw weighted outcomes |
| `import`; `par` → `par import`, `par export` | `import` dispatches by recognized source; legacy `stakeengine import` is not public | Import Stake files / Exchange a PAR workbook |
| `inspect`, `sample` | legacy `outcomesource inspect|sample` is not public; Stake remains analyze-only | Recognize/open a project / Inspect, sample or analyze precomputed outcomes |
| `reel` → `reel generate` | specialised Blueprint materialization route | Generate/inspect reel strips |
| `replay`, `sim` | source-specific runtime versus pre-generated semantics are not aliases | Replay a result / Simulate |
| `validate` | legacy `outcomelibrary validate` is not public; WASM remains metadata-only | Validate artifacts |

| Internal-only route | Why it is not public | Matrix operation reached through the public route |
| --- | --- | --- |
| `outcomelibrary build`, `outcomelibrary generate`, `outcomelibrary validate` | delegated implementation namespace; public `generate` owns raw JSON generation, while public `build` (recognized project source) and `export` (descriptor/raw source) own canonical bundle materialization and `validate` owns validation | Generate raw weighted outcomes / Materialize a canonical Outcome Library bundle / Validate artifacts |
| `outcomesource inspect`, `outcomesource sample`, `outcomesource diff` | delegated implementation namespace; public `inspect`/`sample`/`diff` own its user contract | Recognize/open / Inspect, sample or analyze outcomes / Compare reports or outcome sources |
| `stakeengine export`, `stakeengine import`, `stakeengine analyze`, `stakeengine diff` | delegated implementation namespace; public `export`/`import`/`report`/`diff` own its user contract; analyze/diff `--out` files are separate result artifacts | Export/import Stake / Inspect, sample or analyze outcomes / Compare reports or outcome sources |
| `name`, `studio`, `__studio` | utility and Studio-launcher implementation paths; neither is advertised in the public command tree | no public domain route |

| Studio domain route | Alias / duplicate route treatment | Matrix operation |
| --- | --- | --- |
| Home Design / create, Design editor, Projects register/open | project lifecycle routes | Create an editable game design / Edit a design / Recognize/open a project |
| Build/Export, Outcome Libraries Generate/Build-Export, Stake Export, PAR import/export | Studio Generate combines raw generation and canonical bundle materialization in one request; PC-16 owns duplicate-control sweep after the product-domain and public-CLI owners close their own contracts | Build / Generate raw weighted outcomes + Materialize a canonical Outcome Library bundle / Export Stake / Exchange PAR |
| Play, Simulation, Simulation results, Replay | runtime versus outcome-source paths have separate guarantees | Run/play / Simulate / Render/compare / Replay |
| Blueprint check, Outcome/Stake export validate, Certification build/inspect/export | validation/certification control sweep | Validate artifacts / Certify an Outcome Library |
| Provably Fair configure/generate/verify, Export/Deploy, reel tools, WASM register/open/inspect | specialised product boundaries | Prove fairness / Deploy / Generate reels / Assess WASM |

## Closure ledger: mismatches and duplicate paths

| ID | Classification | Frozen evidence / architecture fact | Current-base status | Owning remediation or sweep | Completion condition |
| --- | --- | --- | --- | --- | --- |
| PC-05-CLI-01 | false prerequisite | PC-02: `inspect` says Blueprint must build before simulate; direct Blueprint `sim` succeeds | Unresolved capability contradiction | PC-06 diagnostic sweep | one canonical capability decision; inspect/help/runtime agree and test both source paths |
| PC-05-CLI-02 | incompatible-input diagnostic | PC-02: Outcome Library passed to `import` yields only `stakeengine-import-index-missing` | Unresolved input-kind recovery diagnostic | PC-06 diagnostic sweep | error names supplied kind, accepted input kinds, and recovery without exposing parser file names |
| PC-05-CLI-03 | duplicated command rendering | PC-02: create’s suggested Build command does not quote a spaced path | Unresolved generated-command rendering | PC-15 public CLI/help/docs sweep | every generated command is shell-safe for the actual supplied path; regression includes spaces |
| PC-05-CLI-04 | previously remediated public grammar | **Frozen observation (immutable):** PC-02 recorded that `import --help` advertised `--format` while the delegated handler rejected it. | **Previously remediated:** PC-04 commit `afb072d4523b65d04166b4ac53e1ff34f3dfd3bf`; `ImportCommand` forwards `--format json`, `StakeEngineCommand` accepts it and produces the JSON import report, and `ImportCommand.test.ts` asserts that observable result. | Previously remediated in PC-04; no PC-06 closure work | public `import --format json` and delegated Stake import retain one parser/help/output contract |
| PC-05-STUDIO-01 | import replacement/recovery | PC-03 frozen P2: saved-design selection can replace editable starter with invalid/blank state without explanatory import error | Unresolved Studio replacement/recovery path | PC-16 Studio recovery sweep | selection validates before replacement or retains prior design; visible recovery explains outcome |
| PC-05-STUDIO-02 | certification validation asymmetry | Studio can build certification but CLI alone verifies an already-built bundle against source | Unresolved Studio verification handoff | PC-11 Studio validation/certification sweep | explicitly ship Studio verification or label/route the external CLI handoff; docs/matrix agree |
| PC-05-HANDOFF-01 | raw-generation-to-bundle handoff | Public `generate --out` delegates to `outcomelibrary generate` and writes raw `WeightedOutcomeLibrary` JSON; a later `outcomelibrary build` consumes descriptor-named raw JSON to write the canonical bundle. Studio Generate combines both stages. | Unresolved public-to-internal materialization handoff | PC-09 Outcome Library sweep | raw generation, checkpoint/resume, descriptor build and Studio's combined path state the same provenance, stale, compatibility and recovery boundary without calling raw JSON a bundle |
| PC-05-DUP-01A | Outcome Library conversion overlap | `build --target outcomeLibrary` and `export --to outcomes` materialize canonical bundles; `generate` is a distinct raw-library producer whose handoff is PC-05-HANDOFF-01 | Unresolved Outcome Library conversion contract | PC-09 Outcome Library sweep | one Outcome Library contract defines exact-enumeration prerequisites, raw-to-bundle materialization, bundle validation and loss/diagnostics |
| PC-05-DUP-01B | Stake conversion overlap | `build --target stakeAdapter`, `export --to adapter`, and delegated `stakeengine export` reach Stake export | Unresolved Stake export conversion contract | PC-10 Stake export sweep | Stake export has one prerequisite/provenance/output contract |
| PC-05-DUP-01C | PAR conversion overlap | `par export`, `par import`, and `export --to workbook` overlap around the PAR exchange boundary | Unresolved PAR conversion contract | PC-11 PAR conversion sweep | PAR exchange retains one explicit import-before-run and loss/compatibility contract |
| PC-05-DUP-01D | public CLI conversion aliases | public `build`, `generate`, `export`, and `par` spell overlapping conversion goals | Unresolved public CLI route consolidation | PC-15 public CLI/help/docs sweep | one documented CLI route per goal; retained aliases delegate with identical defaults, validation and diagnostics |
| PC-05-DUP-01E | Studio conversion duplicates | Build/Export, Outcome Libraries, Stake Export and PAR panels expose overlapping Studio conversion controls | Unresolved Studio conversion consolidation | PC-16 Studio conversion sweep | Studio exposes one discoverable control per goal or explicit delegation with matching disabled/recovery states |
| PC-05-DUP-02 | runtime/outcome-source duplication | package runtime and native outcome source share sim/replay/serve nouns with different guarantees | Unresolved public semantic distinction | PC-06 provenance sweep | every surface labels execution vs pre-generated selection and exact vs best-effort replay |
| PC-05-DUP-03A | CLI validation contract | CLI and Studio expose different subsets of Blueprint/library/Stake/certification checks | Unresolved CLI validation contract | PC-06 CLI validation sweep | CLI reports target-specific checks and exclusions through one contract |
| PC-05-DUP-03B | Studio validation duplication | CLI and Studio expose different subsets of Blueprint/library/Stake/certification checks | Unresolved Studio validation control sweep | PC-11 Studio validation sweep | capability matrix is reflected in Studio controls, disabled states and explicit CLI handoffs |
| PC-05-DOC-01A | documentation drift risk | `ProjectPresentation`, generated actions, CLI help and public docs can state prerequisites independently | Unresolved public-documentation alignment | PC-15 public CLI/help/docs sweep | docs, generated actions and help derive/verify against the shared capability contract |
| PC-05-DOC-01B | WASM boundary drift risk | WASM inspection-only claims cross docs, resolver and Studio registration | Unresolved WASM boundary alignment | PC-13 WASM boundary sweep | docs and surfaces agree that WASM has no POKIE producer/runtime/logic-validation promise |

The owner labels are future immutable steps, not product changes made by PC-05.
