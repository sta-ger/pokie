# PC-05 capability matrix

This is the acceptance contract for product coherence, frozen after the PC-02,
PC-03, and PC-04 evidence.  The machine-readable artifact vocabulary is
[`artifact-registry.json`](artifact-registry.json).  A dash means the surface
does not expose the operation; it is not evidence that the operation works by
some other route.

| Domain operation / user goal | CLI | Studio | Public docs | Shared contract / defaults | Prerequisite and validation | Diagnostic semantics | Owner |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Create an editable game design | `create` (`--blank`, `--random`, wizard) | Home Design / create | `getting-started.md` | `GameBlueprint`; default output derives from id | valid blueprint required before dependent builds | file conflict: no overwrite; invalid fields are actionable | No open mismatch — baseline operation is already covered by its current contract |
| Scaffold a ready-to-run project | `init <directory>` | Design / Build project creation flow | `getting-started.md`, `game-packages.md` | starter Blueprint materialized as a local TypeScript package with the running POKIE version | chosen empty/safe directory; local dependency installation | unsafe/existing destination is refused before scaffold replacement; installed version is stated | No open mismatch — baseline scaffold contract |
| Resume a failed prepared-package lifecycle | `init`'s `GamePackagePreparer` create → dependencies → build → verify lifecycle | — | `docs/cli.md` `init` preparation/retry guidance | retry-only `.pokie-prepare-state.json` records preparer-owned manifest, created files and completed phases; it is neither a project nor a conversion input | marker is always at the fixed project-root companion path; it proves only that this preparer created the partial scaffold | failed dependencies/build/verify names the phase and tells the author to fix it then retry the same parent/name; missing/corrupt marker starts normally and an unrelated existing directory still refuses overwrite | No open mismatch — PC-05 inventories the preparation recovery companion; successful verify removes it |
| Edit a design | `edit` | Design editor | `getting-started.md` | canonical Blueprint JSON | load then validate before save/build | edited source invalidates results | No open mismatch — the separate PC-05-STUDIO-01 replacement finding owns only its observed recovery defect |
| Recognize/open a project | `inspect`, path-taking commands | Projects / register/open | `docs/README.md`, `game-packages.md` | `ProjectTargetResolver`, `ProjectType` | resolver classifies Blueprint/package/library/Stake/PAR/WASM | name the recognized type and next action; do not leak parser internals | PC-06 diagnostic sweep |
| Exchange a PAR workbook | `par export`, `par import`; `export --to workbook` | PAR import/export panel | `stake-engine-import.md` is not authoritative; PAR docs/examples are | `ParSheetExporter`/`ParSheetImporter`; XLSX | Blueprint → workbook → Blueprint | workbook is exchange-only; say import-before-run | PC-11 PAR conversion sweep; PC-15 public CLI sweep; PC-16 Studio sweep |
| Attach Studio symbol artwork | — | Design editor Symbols picker imports a PNG, keeps an `assets/symbols/...png` reference in `Blueprint.symbolArtwork`, and materializes the staged file on Save/Save managed; `CanonicalPlayerView` resolves that declared reference through `/api/project/symbol-artwork` and supplies its URL to the shared renderer | no dedicated public document: the Studio design workflow must describe this as an optional persisted companion, not embedded Blueprint JSON | `StudioBlueprintService.importSymbolArtwork()` / `materializeSymbolArtwork()`; `SymbolPresentation` and Studio's `CanonicalPlayerView` use declared references only; `cli/client/player/renderPlayer` only renders an optional caller-supplied `artworkUrlForSymbol` callback and does not consume a Studio endpoint | selected regular PNG, signature verified, ≤5 MB; save confines the companion under the Blueprint directory and the Studio endpoint serves only declared safe references | missing, stale, undeclared or unsafe files return no image/404 and retain the symbol-id fallback; re-import and save, or remove the stale reference | No open mismatch — PC-05 inventory contract records the durable Studio companion lifecycle |
| Reuse a managed compatible Outcome Library | `build`/`export` lifecycle can obtain a managed library; no separate public registry command | Studio and CLI share `ManagedOutcomeProjectService`, not private generated-output indexes | internal lifecycle contract; not an interchange format | `.pokie/managed-outcome-projects.json` records canonical bundle locations plus exact compatibility provenance | manifest reader, resolver and game/version/configuration/POKIE-version/generation comparison must all agree before reuse | missing/stale/corrupt target entries are never reused; malformed registry reads surface for repair, while a structurally wrong `projects` field is empty; rerun managed generation/export to register or adopt a verified deterministic bundle | No open mismatch — durable metadata companion inventoried by PC-05 |
| Discover Studio custom Outcome Library bundles across restart | — | Outcome Library Registry reads the default bundle and custom Studio Generate destinations | Studio implementation contract; no public CLI equivalent | `.pokie/outcome-library-registry.json` is a directory-name discovery index, never outcome data | containment/realpath and directory checks filter index entries; manifest then classifies compatible/stale/wrong | corrupt/missing/unsafe index entries fail open to the default bundle; a safe indexed corrupt bundle reports its own load error; regenerate or repair/remove the index to recover | No open mismatch — durable metadata companion inventoried by PC-05 |
| Persist and recover Studio Projects | — | Projects dashboard create/register/open/remove/relocate and restart | Studio implementation contract; no public CLI equivalent | `FileStudioProjectRegistry` atomically reads/writes app-data `projects.json`; `StudioProjectRegistrationService` canonicalizes, re-resolves and exposes status | fixed file below `PokiePathResolver.resolveAppDataDirectory()`; recognized project locations canonicalize by realpath/absolute fallback, and every list recalculates missing state | missing file starts fresh; corrupt/non-array content is empty rather than startup failure; missing project remains visible, explicit relocate preserves provenance, remove deletes only its registry row, and unavailable app-data falls back to process-local memory | No open mismatch — durable app-data registry contract inventoried by PC-05 |
| Persist a dev-server session across restart | `serve` itself always uses process-local `InMemorySessionRepository`; application embedding configures `new PokieDevServer(game, {sessionRepository: new FileSessionRepository(directory)})` | — | `docs/cli.md` Session storage & wallet; root `pokie` export | default is in-memory Map and loses sessions on restart. Opt-in `FileSessionRepository` writes one SHA-256(sessionId)-named JSON state record under its configured directory and implements additive versioning | server `POST /sessions` and committed spins write serializable state; `GET /sessions/:id` and post-restart spin cache misses read it. Valid legacy raw state is version 0 and upgrades on save; configured directory owns storage and hash filenames contain untrusted ids | missing/corrupt record maps to unknown-session 404, not a crash; valid records restore after restart but credits/idempotency defaults do not. Same-instance queues/version checks help locally, but no OS lock closes a two-process shared-directory race; use an externally locked/transactional store when that guarantee is needed | No open mismatch — supported opt-in durable server integration, explicitly not a project/replay artifact |
| Build a runnable package | `build --target tsPackage` | Build/Export artifact target | `game-packages.md` | `ArtifactBuilderRegistry`, `BUILD_PRODUCT_MATRIX`; default sibling `tsPackage` | Blueprint; `GameBlueprintValidator`, materialization | unsupported source uses shared matrix diagnostic | PC-07 package build sweep; PC-15 public CLI sweep; PC-16 Studio sweep |
| Generate raw weighted outcomes | `generate --out <file>`; legacy/internal `outcomelibrary generate --out <file>` | no raw-file-only Studio route: Studio Generate immediately materializes a bundle | `outcome-library-bundle.md` | `generateExactWeightedOutcomeLibrary` produces one `WeightedOutcomeLibrary` JSON value; `--resume` persists an `ExactEnumerationCheckpoint` only on cancellation | runnable package must support the selected exact/sampled enumeration; checkpoint resumes only the same compatible generation | raw JSON is an intermediate result, not a native bundle; cancellation says whether a checkpoint was written and how to resume | PC-09 Outcome Library sweep; PC-15 public CLI sweep |
| Materialize a canonical Outcome Library bundle | `build --target outcomeLibrary` from a recognized compatible project, `export <config.json> --to outcomes` from an Outcome Library bundle descriptor; legacy/internal `outcomelibrary build` | Dashboard Build/Export Outcome Library card delegates to canonical artifact/generation services; the retired Outcome Libraries tab is not an owner. | `outcome-library-bundle.md` | canonical `outcomeLibraryBundleDescriptor`: each mode names exactly one raw `libraryPath` JSON or canonical streamed `outcomesPath` JSONL (with libraryId); the separate `canonicalOutcomeJsonl` source is read by `OutcomeLibraryCommand.loadDescriptor()`/stream reader, and `ExportCommand` delegates unchanged | relative inputs resolve from the descriptor directory; descriptor/source shape, JSONL parsing, cross-mode provenance and bundle writer validation must pass; neither raw JSON nor streamed JSONL alone is sample/serve/replay capable | name malformed mode/source, missing JSONL libraryId and config path; target export says outcomes source is incompatible and tells the user to repair/repoint stale stream/library sources and use a safe destination; do not advertise `generate` as already having built a directory bundle | PC-11 retained owner; PC-09 Outcome Library sweep; PC-15 public CLI sweep; PC-16 Studio sweep |
| Inspect, sample or analyze precomputed outcomes | `inspect`, `sample`, `report --out`, `diff --out`; legacy/internal `outcomesource inspect/sample/diff --out`, `stakeengine analyze/diff --out` | Outcome source overview / Inspect, Sample, report comparison | `outcome-library-bundle.md`, `stake-engine-standalone.md` | `OUTCOME_SOURCE_*` capabilities and standalone Stake analyzer | native library can sample; native library and Stake export can analyze/diff | clearly distinguish read-only Stake source from drawable native library; persisted analyses/diffs are result records, never source projects | PC-06 diagnostic sweep |
| Export Stake files | `build --target stakeAdapter`, `export <config.json> --to adapter`; legacy/internal `stakeengine export` | Build/Export and Stake Export | `stake-engine-export.md` | CLI uses the canonical `stakeEngineExportDescriptor`: each mode has a name/cost and exactly one raw `libraryPath` or native `bundleDir` (optional `bundleModeName`), delegated by `ExportCommand` to `StakeEngineCommand.loadDescriptor()`. Studio instead uses the shared transient `studioOutcomeLibrarySelector` (`json`/`bundle`/`stakeengine`) per mode, through `loadOutcomeLibraryFromSelector()`; it is not a CLI descriptor. | CLI relative inputs resolve from the descriptor directory and pass `StakeEngineExportValidator`; Studio confines selectors to the project, requires named selector mode equality, and rejects a canonical bundle whose manifest configHash is stale against the current project before export. Raw/bundle/Stake provenance can all become stale; Stake publication remains its own recognized-output/overwrite contract. | CLI config/source errors identify the descriptor; Studio reports selector containment/import/mode mismatch and stale-bundle regeneration guidance before exporter validation. Repair/restore the selected source, choose the matching mode, regenerate a stale bundle, then retry; do not silently downgrade a bundle/Stake selection to raw JSON. | PC-10 Stake export sweep; PC-15 public CLI sweep; PC-16 Studio sweep |
| Import Stake files | `import` (public); legacy/internal `stakeengine import` | Project registration/open only; no equivalent conversion action | `stake-engine-import.md` | `StakeEngineImporter` reconstructs an Outcome Library plus the POKIE-created `stakeImportReExportConfig` and optional source-provenance companions | POKIE-produced export needs `pokie-manifest.json`; generic Stake is analysis-only | the import `config.json` is a bundleDir-only convenience specialization, not the generic user-authored Stake descriptor; moved/malformed companion follows normal export diagnostics and can be recreated by re-import or repaired | PC-06 diagnostic sweep; PC-16 consolidation sweep |
| Validate artifacts | `validate --out <file>` writes a `ValidateReport`; legacy/internal `outcomelibrary validate`; `certification verify` | Overview project diagnostics plus target-specific Blueprint/PAR/Outcome/Stake/deployment/certification source validation. Studio hands existing-evidence verification to `pokie certification verify <certDir> --source <bundleDir>`. | artifact-specific docs | resolver plus validators; `--deep` applies to libraries | target-specific validation only; certification verification requires the same live unchanged source bundle | state what was checked and what is not checked (notably WASM/runtime); a saved ValidateReport is not a project input and must be rerun after source changes. Certification handoff says to rebuild when source/evidence changed. | PC-11 Studio validation/certification owner; PC-06 CLI contract sweep |
| Run/play a game | `serve`, `client`, `dev` | Play | `game-packages.md`, `pregenerated-runtime.md` | `RUNTIME_EXECUTE_CAPABILITY` or native outcome-source sampling; Blueprint runtime adapters (`dev`/`serve` and Studio Home/Play) resolve through `createMaterializingRuntimePackageResolver` | runnable package or native Outcome Library, mode when required; a Blueprint is privately materialized into the verified `blueprintRuntimeMaterializationCache` with `.pokie-materialized.json` readiness marker | package execution versus pre-generated draw must remain explicit; cache is machine-local runtime state, never a user package/output. Marker-less/stale cache rebuilds under a lock; materialization phase diagnostics identify validation/generate/install/verify and retry is recovery | PC-08 Studio parity |
| Simulate | `sim --out`; `sim --mode all` produces a per-mode SimulationReportSet | Simulation starts a process-local job; completed reports offer JSON/Markdown/HTML HTTP attachments | `getting-started.md`, `pregenerated-runtime.md`, `docs/cli.md` Studio Simulation API | package runtime or `OUTCOME_SOURCE_SAMPLE_CAPABILITY`; CLI `sim` and `StudioSimulationService` use the same Blueprint materializing resolver. `studioSimulationJobState` is not the durable `simulationReport`; `studioSimulationReportDownload` is a completed-report browser envelope | package or native library; a Blueprint receives a verified cached runtime; seeded native simulation is replayable. Only completed retained jobs with reports download; terminal state is process-local, cleared on restart and evicted oldest-first while queued/running jobs remain | reject missing native-library seed/mode when exact replay is promised; stale/marker-less runtime caches rebuild under lock. Download has 400 invalid format, 404 unknown/evicted/different-project id, and 409 queued/running/failed/cancelled-without-report; cancel then rerun, or rerun after restart/eviction. Sanitized attachment filenames and browser-selected save paths never create a project/input | PC-08 runtime/source semantics sweep |
| Render simulation or outcome-source analysis | `report --out <file>` (`--format json` re-emits the parsed SimulationReport/SimulationReportSet; source analysis has its own result type) | Simulation results only | `getting-started.md`, `stake-engine-standalone.md` | simulation-report parser / outcome-source analyzer | simulation report/set or analyzable native/Stake source | a persisted rendering/analysis is presentation, never a project artifact; source-specific JSON is not interchangeable with a simulation report | PC-08 Studio parity |
| Compare reports or outcome sources | `diff --out <file>`; legacy/internal `outcomesource diff --out`, `stakeengine diff --out` | Simulation results only | `getting-started.md`, `stake-engine-standalone.md` | simulation differ/set differ, outcome-source differ and standalone Stake differ | like-with-like simulation reports/sets, or two analyzable sources of the same command contract | persisted diff is historical when either input changes; source diffs never create a drawable source and create-only branches refuse overwrite | PC-06 diagnostic sweep |
| Replay a result | `replay --out` | Replay (seed, artifact, Session Spin, simulation sources): retained completed jobs link the HTTP replay-descriptor attachment; Session Spin builds a selected-view JSON Blob in the browser | `pregenerated-runtime.md`, `round-artifacts.md`, `docs/cli.md` Studio Replay API | exact native descriptor versus best-effort package replay; `studioReplayJobState` and 20-entry `StudioRoundRecorder` history are process-local, while `studioReplayDownload` wraps the canonical descriptor and `studioSessionSpinDownload` wraps only a selected session view | library id/hash/mode/seed/round must match for exact result; Blueprint replay borrows the verified private cache. Only completed retained jobs have descriptors; Session Spin is selected only while history retains it | mismatch identifies stale/incompatible provenance; marker-less/stale cache rebuilds under lock and package replay says best effort. Failed/cancelled/active descriptor download is 409, unknown/evicted/restarted/project-switched id is 404; cancellation/retry reruns a job. Blob/attachment suggested filenames do not select a server path or create a project/conversion prerequisite | PC-06 provenance sweep |
| Certify an Outcome Library | `certification build <bundleDir> <config.json>`, `certification verify` | Certification build/inspect/export; after a successful build Studio shows `pokie certification verify <certDir> --source <bundleDir>`. Current `buildResult` can download only its manifest as a browser JSON Blob. | `certification-evidence-bundle.md`, `docs/cli.md` Studio Certification | canonical `certificationBuildDescriptor` is user-authored and consumed by `CertificationCommand.loadDescriptor()` before `buildFromBundle`; `CertificationEvidenceBundle*` is durable output. `studioCertificationBuildState`/`studioCertificationManifestDownload` are process-local/browser delivery companions | native Outcome Library plus descriptor modes with string modeName/seed and numeric sampleCount; the handoff verifier requires the same live, unchanged source. Manifest blob is available only from the retained current successful build result and never substitutes for the evidence directory | malformed config identifies path/mode/config hint; wrong recognized source gets capability diagnostic; no buildResult disables export. Rebuild after source/output changes before verification; browser filename is only a suggestion and the manifest Blob is neither a project, conversion prerequisite nor replay input | PC-11 Studio validation/certification owner |
| Prove fairness | `fairness seed-commit/commit/reveal/verify` | Provably Fair configure/generate/verify | `provably-fair.md` | commitment/proof validators and verifier | private seed file, native library, mode; matching commitment/source for verify | never disclose private seed; mismatch fails closed | PC-06 provenance sweep |
| Deploy an external format | no first-class public CLI command | Export/Deploy | `external-adapter-sdk.md`; `docs/cli.md` Studio API currently (incorrectly) documents `libraryPath` rather than the live `librarySelector` request | Studio uses `studioOutcomeLibrarySelector` (`json` raw file, `bundle` named canonical mode, or `stakeengine` named Stake-export mode) for each row, then one `ExternalDeploymentService` pipeline and registered target. `publish:false` previews by removing runtime delivery; `publish:true` permits the target to publish. | Studio requires an inspectable current build and only its current mode ids, named selector-mode equality, project-root containment and source resolution before SDK descriptor/compatibility/projection/generation/artifact/target checks. The supplied local JSON target owns `deployment/local-json-example`; it atomically replaces only a prior recognized target output, with rollback. | Unknown target, no/stale current build, mode mismatch and selector load errors are explicit before the SDK. The run view preserves target-stage diagnostics. Preview never writes; on local delivery, write/swap failure preserves or restores the prior directory, rollback failure names the stale backup, and cleanup failure is a post-success warning. Repair/rebuild/reselect, preview, then publish. | PC-16 Studio deployment/docs-surface sweep — API docs request-shape mismatch remains open; PC-05 does not rewrite public docs |
| Generate/inspect reel strips | `reel generate` | Blueprint reel tools | `reel-strip-generation.md` | `resolveReelStripGeneration` | Blueprint constraints | generation failure is design validation, not runtime failure | No open mismatch — baseline design-validation operation |
| Assess WASM compatibility | `inspect` resolves compatible sidecar; no `wasm` command | register/open/inspect metadata only | `wasm-compatibility-boundary.md` | `WasmProjectTargetAdapter`, manifest validator | compatible `.wasm` + sidecar manifest | inspection-only; explicitly reject build/run/simulate/validate-logic promises | PC-13 WASM boundary sweep |

`artifact-registry.json` also carries the machine-readable
`persisted_public_output_contracts` inventory. It records every public
`--out`/`--resume` persistence branch and every branch that writes a default
destination, its producing command source and the artifact kind it writes.
The default audit includes `init`, confirmed `edit`, conditional reel
`--apply`/`--materialize`, PAR and dispatched import defaults, target-specific
`build` and `export` defaults, Stake import and certification. Result records
are intentionally listed alongside source artifacts so validation, comparison,
analysis and report JSON cannot silently disappear from the lifecycle model
merely because no later POKIE command consumes them.

## Public route inventory and aliases

The public CLI inventory is the command tree registered by `registerCliCommands()`.
The implementation-only names below are recorded only to make duplicate delegation
auditable: they are deliberately not supported public commands. The registered
`__studio` adapter is likewise internal; its concrete `studio` launcher is not a
public CLI alias. A row in the main matrix names the destination semantics for
every public route.

| Public CLI route | Alias / duplicate route treatment | Matrix operation |
| --- | --- | --- |
| `build` | target-specific product remediation belongs to PC-09 (Outcome Library), PC-10 (Stake) and PC-11 (PAR); omitting `--out` writes the target-named sibling after compatibility/destination checks; public CLI aliases are PC-15 and Studio duplicates are PC-16 | Build a runnable package / Materialize a canonical Outcome Library bundle / Exchange a PAR workbook / Export Stake files |
| `certification` → `certification build`, `certification verify` | distinct build and independent verification routes | Certify an Outcome Library |
| `client`, `dev`, `serve` | `dev` composes the two local browser/runtime routes | Run/play a game |
| `create`, `edit`, `init` | create defaults an id-derived Blueprint after confirmation; edit defaults its Save to the input Blueprint but remains an interactive confirmed choice; init writes/merges its TypeScript package in the supplied/current safe directory | Create an editable game design / Edit a design / Scaffold a ready-to-run project |
| `diff`, `report` | `diff --out` persists one of the separately typed comparison results; `report --out` persists a simulation rendering or outcome-source analysis. Legacy `outcomesource diff` / `stakeengine analyze|diff` delegate to these analysis goals | Inspect, sample or analyze precomputed outcomes / Render simulation or outcome-source analysis / Compare reports or outcome sources |
| `export` | for descriptor sources, `--to outcomes` delegates to `OutcomeLibraryCommand.loadDescriptor()` and `--to adapter` to `StakeEngineCommand.loadDescriptor()`; the two durable user-authored contracts remain distinct from the POKIE-created Stake-import config companion. Each non-dry-run target has an audited default destination when `--out` is absent | Materialize a canonical Outcome Library bundle / Export Stake files / Exchange a PAR workbook |
| `fairness` → `fairness seed-commit`, `fairness commit`, `fairness reveal`, `fairness verify` | commit/reveal lifecycle, not conversion aliases | Prove fairness |
| `generate` | canonical public raw `WeightedOutcomeLibrary` JSON generation. Its `--out` file is an input to a later descriptor-based `export --to outcomes` bundle materialization; it does not create a bundle. Legacy `outcomelibrary generate|build` is not public | Generate raw weighted outcomes |
| `import`; `par` → `par import`, `par export` | `import` dispatches by recognized source and preserves the delegate's default output when `--out` is absent; PAR import/export defaults are sibling `.blueprint.json`/`.par.xlsx` destinations | Import Stake files / Exchange a PAR workbook |
| `inspect`, `sample` | legacy `outcomesource inspect|sample` is not public; Stake remains analyze-only | Recognize/open a project / Inspect, sample or analyze precomputed outcomes |
| `reel` → `reel generate` | specialised Blueprint materialization route; only successful `--apply` or `--materialize` writes, defaulting to the input Blueprint when `--out` is absent | Generate/inspect reel strips |
| `replay`, `sim` | source-specific runtime versus pre-generated semantics are not aliases | Replay a result / Simulate |
| `validate` | legacy `outcomelibrary validate` is not public; WASM remains metadata-only | Validate artifacts |

| Internal-only route | Why it is not public | Matrix operation reached through the public route |
| --- | --- | --- |
| `outcomelibrary build`, `outcomelibrary generate`, `outcomelibrary validate` | delegated implementation namespace; public `generate` owns raw JSON generation, while public `build` (recognized project source) and `export` (the canonical Outcome Library bundle descriptor) own canonical bundle materialization and `validate` owns validation | Generate raw weighted outcomes / Materialize a canonical Outcome Library bundle / Validate artifacts |
| `outcomesource inspect`, `outcomesource sample`, `outcomesource diff` | delegated implementation namespace; public `inspect`/`sample`/`diff` own its user contract | Recognize/open / Inspect, sample or analyze outcomes / Compare reports or outcome sources |
| `stakeengine export`, `stakeengine import`, `stakeengine analyze`, `stakeengine diff` | delegated implementation namespace; `stakeengine export` consumes the generic Stake Engine export descriptor, while public `export --to adapter` delegates that same contract. Stake import writes only its bundleDir-only config companion; analyze/diff `--out` files are separate result artifacts | Export/import Stake / Inspect, sample or analyze outcomes / Compare reports or outcome sources |
| `name`, `studio`, `__studio` | utility and Studio-launcher implementation paths; neither is advertised in the public command tree | no public domain route |

| Studio domain route | Alias / duplicate route treatment | Matrix operation |
| --- | --- | --- |
| Home Design / create, Design editor, Projects register/open | project lifecycle routes; Symbols imports stage a PNG outside Blueprint JSON and the next Save materializes it under the active Blueprint; `CanonicalPlayerView` resolves the Studio endpoint URL and passes it to the shared callback-only renderer | Create an editable game design / Edit a design / Attach Studio symbol artwork / Recognize/open a project |
| Build/Export, Outcome Libraries Generate/Build-Export, Stake Export, PAR import/export | Studio Generate combines raw generation and canonical bundle materialization in one request, then best-effort records its project-contained custom bundle directory in `.pokie/outcome-library-registry.json` for restart discovery; PC-16 owns duplicate-control sweep after the product-domain and public-CLI owners close their own contracts | Build / Generate raw weighted outcomes + Materialize a canonical Outcome Library bundle / Export Stake / Exchange PAR / Discover Studio custom Outcome Library bundles across restart |
| Play, Simulation, Simulation results, Replay | runtime versus outcome-source paths have separate guarantees | Run/play / Simulate / Render/compare / Replay |
| Blueprint check, Outcome/Stake export validate, Certification build/inspect/export | validation/certification control sweep | Validate artifacts / Certify an Outcome Library |
| Provably Fair configure/generate/verify, Export/Deploy, reel tools, WASM register/open/inspect | specialised product boundaries | Prove fairness / Deploy / Generate reels / Assess WASM |

## Closure ledger: mismatches and duplicate paths

| ID | Classification | Frozen evidence / architecture fact | Current-base status | Owning remediation or sweep | Completion condition |
| --- | --- | --- | --- | --- | --- |
| PC-05-CLI-01 | false prerequisite | PC-02: `inspect` says Blueprint must build before simulate; direct Blueprint `sim` succeeds | Unresolved capability contradiction | PC-06 diagnostic sweep | one canonical capability decision; inspect/help/runtime agree and test both source paths |
| PC-05-CLI-02 | incompatible-input diagnostic | PC-02: Outcome Library passed to `import` yields only `stakeengine-import-index-missing` | Unresolved input-kind recovery diagnostic | PC-06 diagnostic sweep | error names supplied kind, accepted input kinds, and recovery without exposing parser file names |
| PC-05-CLI-03 | duplicated command rendering | PC-02: create’s suggested Build command does not quote a spaced path | Unresolved generated-command rendering | PC-15 public CLI/help/docs sweep | every generated command is shell-safe for the actual supplied path; regression includes spaces |
| PC-05-CLI-04 | previously remediated public grammar | **Frozen observation (immutable):** PC-02 recorded that `import --help` advertised `--format` while the delegated handler rejected it. | **Previously remediated:** PC-04 commit `afb072d4523b65d04166b4ac53e1ff34f3dfd3bf`; `ImportCommand` forwards `--format json`, `StakeEngineCommand` accepts it and produces the JSON import report, and `ImportCommand.test.ts` asserts that observable result. | Previously remediated in PC-04; no PC-06 closure work | public `import --format json` and delegated Stake import retain one parser/help/output contract |
| PC-05-STUDIO-01 | import replacement/recovery | PC-03 frozen P2: saved-design selection can replace editable starter with invalid/blank state without explanatory import error | Unresolved Studio replacement/recovery path | PC-16 Studio recovery sweep | selection validates before replacement or retains prior design; visible recovery explains outcome |
| PC-05-STUDIO-02 | certification validation asymmetry | Studio can build certification but CLI alone verifies an already-built bundle against source | Unresolved Studio verification handoff | PC-11 Studio validation/certification sweep | explicitly ship Studio verification or label/route the external CLI handoff; docs/matrix agree |
| PC-05-HANDOFF-01 | raw-generation-to-bundle handoff | Public `generate --out` delegates to `outcomelibrary generate` and writes raw `WeightedOutcomeLibrary` JSON; a later `outcomelibrary build` consumes descriptor-named raw JSON to write the canonical bundle. Studio Generate combines both stages. | Unresolved public-to-internal materialization handoff | PC-09 Outcome Library sweep | raw generation, checkpoint/resume, descriptor build and Studio's combined path state the same provenance, stale, compatibility and recovery boundary without calling raw JSON a bundle |
| PC-05-DUP-01A | Outcome Library conversion overlap | `build --target outcomeLibrary` and `export --to outcomes` materialize canonical bundles; `generate` is a distinct raw-library producer whose handoff is PC-05-HANDOFF-01 | Unresolved Outcome Library conversion contract | PC-09 Outcome Library sweep | one Outcome Library contract defines exact-enumeration prerequisites, raw-to-bundle materialization, bundle validation and loss/diagnostics |
| PC-05-DUP-01B | Stake conversion overlap | `build --target stakeAdapter`, `export --to adapter`, and delegated `stakeengine export` reach Stake export | Unresolved Stake export conversion contract | PC-10 Stake export sweep | Stake export has one prerequisite/provenance/output contract |
| PC-05-STUDIO-03 | shared Studio selector / deployment surface drift | Studio Stake Export and Deployment both resolve `OutcomeLibrarySelector` (`json`/`bundle`/`stakeengine`) through `loadOutcomeLibraryFromSelector()`, while CLI Stake export uses `stakeEngineExportDescriptor`; current `docs/cli.md` deployment API still advertises a `libraryPath` request instead of `librarySelector` | Unresolved Studio selector consolidation and API-documentation mismatch | PC-10 Stake export sweep; PC-16 Studio deployment/docs-surface sweep | Studio consumers retain one selector/provenance/containment/stale contract, CLI descriptor remains explicitly separate, and Studio API docs match the shipped request shape without overstating target delivery semantics |
| PC-05-DUP-01C | PAR conversion overlap | `par export`, `par import`, and `export --to workbook` overlap around the PAR exchange boundary | Unresolved PAR conversion contract | PC-11 PAR conversion sweep | PAR exchange retains one explicit import-before-run and loss/compatibility contract |
| PC-05-DUP-01D | public CLI conversion aliases | public `build`, `generate`, `export`, and `par` spell overlapping conversion goals | Unresolved public CLI route consolidation | PC-15 public CLI/help/docs sweep | one documented CLI route per goal; retained aliases delegate with identical defaults, validation and diagnostics |
| PC-05-DUP-01E | Studio conversion duplicates | Build/Export, Outcome Libraries, Stake Export and PAR panels expose overlapping Studio conversion controls | Unresolved Studio conversion consolidation | PC-16 Studio conversion sweep | Studio exposes one discoverable control per goal or explicit delegation with matching disabled/recovery states |
| PC-05-DUP-02 | runtime/outcome-source duplication | package runtime and native outcome source share sim/replay/serve nouns with different guarantees | Unresolved public semantic distinction | PC-06 provenance sweep | every surface labels execution vs pre-generated selection and exact vs best-effort replay |
| PC-05-DUP-03A | CLI validation contract | CLI and Studio expose different subsets of Blueprint/library/Stake/certification checks | Unresolved CLI validation contract | PC-06 CLI validation sweep | CLI reports target-specific checks and exclusions through one contract |
| PC-05-DUP-03B | Studio validation duplication | CLI and Studio expose different subsets of Blueprint/library/Stake/certification checks | Unresolved Studio validation control sweep | PC-11 Studio validation sweep | capability matrix is reflected in Studio controls, disabled states and explicit CLI handoffs |
| PC-05-DOC-01A | documentation drift risk | `ProjectPresentation`, generated actions, CLI help and public docs can state prerequisites independently | Unresolved public-documentation alignment | PC-15 public CLI/help/docs sweep | docs, generated actions and help derive/verify against the shared capability contract |
| PC-05-DOC-01B | WASM boundary drift risk | WASM inspection-only claims cross docs, resolver and Studio registration | Unresolved WASM boundary alignment | PC-13 WASM boundary sweep | docs and surfaces agree that WASM has no POKIE producer/runtime/logic-validation promise |

The owner labels are future immutable steps, not product changes made by PC-05.
