# Candidate CLI help

At 2026-09-10T06:38:01Z, `node ./dist/cli/pokie.js --help` exited 0.
The rendered command inventory included `build`, `create`, `export`, `import`,
`init`, `par`, `replay`, `report`, `sim`, and `validate`; its next-step text
described both prepared-package and Blueprint workflows.
