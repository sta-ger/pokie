# CLI / Studio / player parity

Studio and the candidate-built public CLI agreed on the generated sibling's base mode identity and library hash. A separate fresh canonical-player profile launched with `node ./dist/cli/pokie.js dev <Studio-built-tsPackage> --no-open`; visible `Start new session` changed the session id, and one `Spin` exposed disabled pending then settled enabled control with credits 1000 → 999 and no Retry surface. Screenshot `screenshots/canonical-player-settled-round.png` and the transcript checksum are retained in the current-candidate delta record.
