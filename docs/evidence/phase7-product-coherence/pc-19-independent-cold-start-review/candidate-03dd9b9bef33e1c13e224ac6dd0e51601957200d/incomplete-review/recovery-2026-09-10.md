# PC-20 focused recovery — exact candidate

Candidate: `03dd9b9bef33e1c13e224ac6dd0e51601957200d`.
The retained exact archive remains `artifacts/pokie-1.3.0.tgz`, SHA-256
`3ba7cde00e67d1205def985ef20958198ec2aca16ee6207e89ee632a57662622`.
The pre-existing verifier-owned freeze receipt still hashes to
`ce24725e1def6bdf673b0c6dca7ddb88255fb5f037260477d87c60688872a92d`.

After one candidate build and public CLI help/invalid-command checks, two new
fresh-profile Studio launches used exactly `node ./dist/cli/pokie.js --no-open`.
The final launch created `starter-slot`, settled one local Spin (`You won
12.00`), then accepted its own Simulation request as `queued — 0/100 rounds`
and later rendered that same local report (`100/100 rounds`, RTP, hit
frequency, and `Open full report`). This later rendered terminal supersedes the
earlier fixed-threshold wait; it is not a product finding.

The repaired native-radio flow selected `Session Spin`, then `Session 1`; the
next rendered item was `Session 1 — Round 1 — Spin — win 12`. It did not expose
a local **Load replay** control, so Replay is not claimed as a completed action.
For each of the enabled exact Outcome Library, TypeScript package, Stake export,
and PAR **Build** controls, the harness retained the ready card, made one
activation, and waited only on that card. No card rendered an accepted/pending
record, local success, or local error. The fresh profile nevertheless contained
the corresponding generated Outcome Library, `tsPackage`, `stakeAdapter`, and
`parWorkbook.xlsx` outputs after the one activations; generated trees are not
retained as evidence.

Thus no immediate action-local terminal failure, missing-object recovery
surface, or correlated P0/P1/P2 product defect was observed. The missing UI
lifecycles prevent a complete PC-19 blind coverage record, post-freeze
comparison, and release validator pass. This remains `inconclusive` in the
`driver` category rather than a product finding.

Verifier-controlled final-launch transcript:
`/home/stager/Work/sta-ger/agents/runtime/verifier-harnesses/PC-20-42fe809212234b68/launch-4-output/studio-transcript.txt`
(SHA-256 `c30ad30a484348df8ee19770b83cb06ce9dafac85f71e341795007ce74b50a80`).
The harness is outside repository evidence; no profiles, output trees, raw
logs, or automation source are committed.
