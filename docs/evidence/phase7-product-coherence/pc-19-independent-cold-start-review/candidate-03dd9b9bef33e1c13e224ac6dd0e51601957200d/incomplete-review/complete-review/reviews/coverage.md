# Independent coverage ledger

The archive-installed public CLI supplied recursive command discovery and
bounded invalid-command behavior. The fresh candidate-source Studio run used a
new profile and rendered controls only. It covered creation, validation,
literal-reel Game Model entry, a live Play round, same-job Simulation lifecycle,
selected-record Replay, exact Outcome Library generation, TypeScript package,
Stake export, and PAR publication.

Those actions exercise artifact conversion and recovery boundaries: local
destinations, generated outcome materialization, single-action completion,
publication receipts, and replay/inspection. The visible Play surface is the
candidate player surface; generated package/player output was reached through
its Studio package action and independently represented by its card-local
output terminal. No retained project or output tree is needed to establish the
rendered lifecycle observations.

Cross-role missions were completed against the corresponding public surfaces:
math/PAR, game frontend/package, QA simulation/report/replay, Outcome Library
integration, Stake/deployment/export, and new project/preparation/retry. The
post-freeze known-findings comparison is recorded separately.
