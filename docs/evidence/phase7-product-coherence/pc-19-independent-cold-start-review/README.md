# PC-19 exact-candidate independent rerun — still incomplete

Candidate: `cc0219a1088de689781de5bf56ed2786e8d40972`  
Candidate package archive: `artifacts/pokie-1.3.0.tgz`  
Archive SHA-256 (rechecked): `07053aad46507e453d0fadd04a18cf47e03031cceb6d1a80ae49ee5735b509ad`

The recovery verifier used fresh-profile public Studio launches from the
candidate source command `node ./dist/cli/pokie.js --no-open`. They completed
Create → saved workspace, New Play session → settled Spin, a 10,000-round
Simulation, Game Model, and exact base Outcome Library generation (1,024
outcomes). A rendered native **Session Spin** source selected and immediately
loaded its recorded round into the local inspector. Focused native-picker flows
also built the rendered TypeScript package, Outcome library, Stake Engine
export, and PAR sheet cards. No action-local product terminal failure was
observed.

The retained exact archive was additionally installed into an isolated consumer
directory and exercised through its public CLI: deterministic Blueprint
creation/validation, exact 91,125-outcome library generation, TypeScript,
Stake, and PAR builds, PAR re-import/validation, Outcome Library simulation and
replay, and fairness seed-commit/commit/reveal/verify all completed. A larger
614,656-combination exploratory exact build emitted its preflight but left no
success record or output, so it is neither a pass nor a product finding.

The remaining uncompleted coverage includes the isolated desktop/narrow player
examples parity review and the cancellation/retry/staging lifecycle variants
required by the complete PC-19 protocol. This is not a PC-19 acceptance pass
and makes no release finding.

The verifier-owned trusted receipt for the prior incomplete freeze remains
outside this mutable evidence directory at
`/home/stager/Work/sta-ger/agents/runtime/verifier-harnesses/PC-20-fc978abcc6fa36ae/pc-20-freeze-receipt.json`,
SHA-256 `9e20aee38195c2714e7492591583a828114ba3106bbdb650d55f79ddb5d992b0`.
It binds the stated candidate and archive digest. The complete-record validator
remains blocked by the absent `PROVENANCE.json`; the next bounded invocation
must complete the remaining role/lifecycle coverage before creating a new
complete freeze and validating the record.
