# PC-19 exact-candidate independent rerun — still incomplete

Candidate: `cc0219a1088de689781de5bf56ed2786e8d40972`  
Candidate package archive: `artifacts/pokie-1.3.0.tgz`  
Archive SHA-256 (rechecked): `07053aad46507e453d0fadd04a18cf47e03031cceb6d1a80ae49ee5735b509ad`

The recovery verifier used its two fresh-profile public Studio launches from the
candidate source command `node ./dist/cli/pokie.js --no-open`. Both completed
Create → saved workspace, New Play session → settled Spin, a 10,000-round
Simulation, Game Model, and exact base Outcome Library generation (1,024
outcomes). The second launch also selected the rendered native **Session Spin**
Replay source and rendered the one available session round. No action-local
product terminal failure was observed.

The first uncompleted boundary is selecting that rendered session round and
loading its replay. TypeScript package/export, generated-sibling fairness,
desktop/narrow examples parity, artifact torture/recovery, CLI parity, and the
remaining role missions were not reached before the two-launch cap. This is not
a PC-19 acceptance pass and makes no release finding.

The verifier-owned trusted freeze receipt remains outside this mutable evidence
directory at
`/home/stager/Work/sta-ger/agents/runtime/verifier-harnesses/PC-20-fc978abcc6fa36ae/pc-20-freeze-receipt.json`,
SHA-256 `9e20aee38195c2714e7492591583a828114ba3106bbdb650d55f79ddb5d992b0`.
It binds the stated candidate and archive digest. The complete-record validator
correctly remains blocked by the absent `PROVENANCE.json`; the next bounded
invocation must continue from Replay Session Spin’s rendered round selection
with the existing stable harness, then create and validate the complete record.
