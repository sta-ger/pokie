# P6-14 exact-candidate capability verification — passed

Candidate verified: `9fe5d3049349bf14cf8de3ece8e1022d37c097ef` (the checked-out candidate before this evidence commit).

The repository's public `npm run test:targeted` workflow ran all eleven requested files as whole files under the locally installed supported Node `v24.18.0` runtime. Jest passed **11/11 suites** and **1,409/1,409 tests** in three configured projects (38.775 s).

[`machine-results.txt`](machine-results.txt) is the concise runner transcript: candidate, runtime, command, every executed path, per-file PASS lines, and Jest's summary. The uncommitted raw runner log was deliberately discarded after recording its SHA-256 there; no generated project, output, browser data, or automation source is included.
