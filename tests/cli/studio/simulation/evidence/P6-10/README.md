# P6-10 host-side Studio verification

Chrome drove the rendered Studio UI over a local Studio server. The bounded
retained proof is the four browser transcripts and five workflow screenshots
in this directory. The package and outcome-library UI runs both completed 24
rounds and exposed reports; their browser transcripts and screenshots are
named by project type. The broad terminal captures used while preparing the
candidate were deliberately removed by the P6V-01 evidence cleanup, because
their claims are not unique to the retained rendered workflow proof.

`browser-transcript-stake-export.txt` and screenshots `03`/`04` record the visible Studio Build/Export flow that generated the canonical library and wrote the Stake project. A fresh Studio server opened on that Stake project; `browser-transcript-stake-guidance.txt` and screenshot `05` show its direct Simulation route’s capability guidance. That rendered guidance contains neither `ENOTDIR` nor a package-path loading error.

The P6V-01 cleanup also removed the broad build transcript. This index makes
no claim about that discarded environment observation; the retained rendered
records are limited to the visible Studio workflows above.
